<Window x:Class="BuildConfigurator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:BuildConfigurator.Views"
        Title="{Binding WindowTitle}"
        Icon="pack://application:,,,/build_configurator.ico"
        Height="700" Width="1100" MinHeight="500" MinWidth="800"
        Background="{StaticResource BrushBackground}"
        Foreground="{StaticResource BrushTextPrimary}"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded"
        Closing="Window_Closing">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ── Tabs ── -->
        <TabControl Grid.Row="0"
                    Background="Transparent"
                    BorderThickness="0"
                    Padding="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Background"    Value="Transparent"/>
                    <Setter Property="Foreground"    Value="{StaticResource BrushTextSecondary}"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding"       Value="16,10"/>
                    <Setter Property="FontSize"      Value="13"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="Bd" Background="Transparent" Padding="{TemplateBinding Padding}">
                                    <Border x:Name="Underline" BorderThickness="0,0,0,2"
                                            BorderBrush="Transparent" Padding="0,0,0,2">
                                        <ContentPresenter ContentSource="Header"/>
                                    </Border>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}"/>
                                        <Setter TargetName="Underline" Property="BorderBrush" Value="{StaticResource BrushAccent}"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
                <Style TargetType="TabPanel">
                    <Setter Property="Background" Value="{StaticResource BrushSurface}"/>
                </Style>
            </TabControl.Resources>

            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <Path Data="M 2 2 H 14 M 2 6 H 10 M 2 10 H 14 M 2 14 H 8"
                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=TabItem}}"
                              StrokeThickness="1.5" Width="16" Height="16" Margin="0,0,6,0"/>
                        <TextBlock Text="Конфигурации"/>
                    </StackPanel>
                </TabItem.Header>
                <views:ConfigurationsTabView DataContext="{Binding ConfigurationsTab}"/>
            </TabItem>

            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal">
                        <Path Data="M 8 2 V 14 M 2 8 H 14"
                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=TabItem}}"
                              StrokeThickness="1.5" Width="16" Height="16" Margin="0,0,6,0"/>
                        <TextBlock Text="Дефайны"/>
                    </StackPanel>
                </TabItem.Header>
                <views:DefinesTabView DataContext="{Binding DefinesTab}"/>
            </TabItem>
        </TabControl>

        <!-- ── Bottom bar: global Save / Reload ── -->
        <Border Grid.Row="1"
                Background="{StaticResource BrushSurface}"
                BorderBrush="{StaticResource BrushBorder}"
                BorderThickness="0,1,0,0"
                Padding="16,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Style="{StaticResource SecondaryButtonStyle}"
                        Width="180" Margin="0,0,8,0"
                        Command="{Binding ReloadAllCommand}">
                    <TextBlock Text="Восстановить с диска"/>
                </Button>
                <Button Style="{StaticResource PrimaryButtonStyle}"
                        Width="150"
                        Command="{Binding SaveAllCommand}">
                    <TextBlock Text="Сохранить на диск"/>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
