<Window x:Class="RemoteLogViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RemoteLogViewer"
        Title="Remote Log Viewer" Height="600" Width="1000" WindowStartupLocation="CenterScreen"
        Closing="Window_Closing">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <Style x:Key="LogTextStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Level}" Value="Warning">
                    <Setter Property="Foreground" Value="{StaticResource LogWarning}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}" Value="Error">
                    <Setter Property="Foreground" Value="{StaticResource LogError}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}" Value="Exception">
                    <Setter Property="Foreground" Value="{StaticResource LogError}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}" Value="Critical">
                    <Setter Property="Foreground" Value="{StaticResource LogCritical}"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}" Value="Fatal">
                    <Setter Property="Foreground" Value="{StaticResource LogCritical}"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="{StaticResource PanelBackground}">
            <Button Content="Add Listener" Click="BtnNewTab_Click"/>
            <Button Content="Settings" Click="BtnManageAddresses_Click"/>
        </StackPanel>

        <TabControl x:Name="MainTabControl" Grid.Row="1" ItemsSource="{Binding Tabs}" SelectedItem="{Binding SelectedTab}">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" ToolTip="{Binding StatusText}">
                        <Ellipse Width="10" Height="10" Fill="{Binding StatusColor}" Margin="0,0,5,0" />
                        <TextBlock Text="{Binding Address}" />
                        <Button Content="X" Width="20" Height="20" Margin="5,0,0,0" Padding="0"
                                Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextSecondary}"
                                Command="{Binding DataContext.CloseTabCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}" />
                    </StackPanel>
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <Grid Background="{StaticResource PanelBackground}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Управление -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
                            <Button Content="Старт" Command="{Binding DataContext.StartCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" />
                            <Button Content="Стоп" Command="{Binding DataContext.StopCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" />
                            <CheckBox Content="Автоконнект" IsChecked="{Binding IsAutoConnect}" VerticalAlignment="Center" Margin="10,0" Foreground="{StaticResource TextPrimary}"/>
                            <CheckBox Content="Автоочистка" IsChecked="{Binding IsAutoClear}" VerticalAlignment="Center" Margin="10,0" Foreground="{StaticResource TextPrimary}"/>
                            <TextBlock Text="{Binding StatusText}" Foreground="{Binding StatusColor}" VerticalAlignment="Center" Margin="10,0" FontWeight="Bold"/>
                        </StackPanel>

                        <!-- Фильтры -->
                        <Expander Grid.Row="1" Header="Фильтры" Foreground="{StaticResource TextPrimary}" Margin="5">
                            <Grid Margin="5">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <WrapPanel Grid.Row="0" Margin="0,0,0,5">
                                    <CheckBox Content="Message" IsChecked="{Binding Filters.ShowMessage}" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextPrimary}" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                                    <CheckBox Content="Warning" IsChecked="{Binding Filters.ShowWarning}" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextPrimary}" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                                    <CheckBox Content="Error" IsChecked="{Binding Filters.ShowError}" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextPrimary}" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                                    <CheckBox Content="Exception" IsChecked="{Binding Filters.ShowException}" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextPrimary}" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                                    <CheckBox Content="Critical" IsChecked="{Binding Filters.ShowCritical}" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextPrimary}" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                                    <CheckBox Content="Fatal" IsChecked="{Binding Filters.ShowFatal}" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextPrimary}" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
                                </WrapPanel>

                                <!-- Категории - список пополняется динамически по мере поступления логов в этой вкладке.
                                     Замок (🔒) - категория гарантированная на движке (LogGeneral/LogEngine/...), но фильтр
                                     этого вьюера всё равно можно применить к ней независимо (см. TabFilters.CategoryVisibility). -->
                                <ItemsControl Grid.Row="1" ItemsSource="{Binding CategoryFilters}" Margin="0,0,0,5">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsVisible, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextPrimary}">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="🔒" Margin="0,0,3,0" Visibility="{Binding IsGuaranteed, Converter={StaticResource BoolToVis}}" ToolTip="Гарантированная категория (не фильтруется рантаймом движка)"/>
                                                    <TextBlock Text="{Binding Name}"/>
                                                </StackPanel>
                                            </CheckBox>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <Grid Grid.Row="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    
                                    <TextBlock Text="Файл:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,5" Foreground="{StaticResource TextSecondary}"/>
                                    <TextBox Text="{Binding Filters.FileFilter, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" Margin="0,0,0,5" TextChanged="Filter_Changed"/>
                                    
                                    <TextBlock Text="Функция:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,5" Foreground="{StaticResource TextSecondary}"/>
                                    <TextBox Text="{Binding Filters.FunctionFilter, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" Margin="0,0,0,5" TextChanged="Filter_Changed"/>

                                    <TextBlock Text="Сообщение:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextSecondary}"/>
                                    <TextBox Text="{Binding Filters.MessageFilter, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="1" TextChanged="Filter_Changed"/>
                                </Grid>
                            </Grid>
                        </Expander>

                        <!-- Таблица -->
                        <DataGrid Grid.Row="2" ItemsSource="{Binding LogsView}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Margin="5" RowDetailsVisibilityMode="Visible" AlternationCount="2">
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                    <EventSetter Event="PreviewMouseLeftButtonUp" Handler="DataGridRow_PreviewMouseLeftButtonUp"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Level}" Value="Warning">
                                            <Setter Property="Foreground" Value="{StaticResource LogWarning}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Level}" Value="Error">
                                            <Setter Property="Foreground" Value="{StaticResource LogError}"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Level}" Value="Exception">
                                            <Setter Property="Foreground" Value="{StaticResource LogError}"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Level}" Value="Critical">
                                            <Setter Property="Foreground" Value="{StaticResource LogCritical}"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Level}" Value="Fatal">
                                            <Setter Property="Foreground" Value="{StaticResource LogCritical}"/>
                                            <Setter Property="FontWeight" Value="Bold"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.RowDetailsTemplate>
                                <DataTemplate>
                                    <Border Visibility="{Binding IsExpanded, Converter={StaticResource BoolToVis}}" Background="{StaticResource PanelBackground}" BorderThickness="2" Margin="10" Padding="10">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="BorderBrush" Value="{StaticResource ControlBorder}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Level}" Value="Warning">
                                                        <Setter Property="BorderBrush" Value="{StaticResource LogWarning}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Level}" Value="Error">
                                                        <Setter Property="BorderBrush" Value="{StaticResource LogError}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Level}" Value="Exception">
                                                        <Setter Property="BorderBrush" Value="{StaticResource LogError}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Level}" Value="Critical">
                                                        <Setter Property="BorderBrush" Value="{StaticResource LogCritical}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Level}" Value="Fatal">
                                                        <Setter Property="BorderBrush" Value="{StaticResource LogCritical}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel>
                                            <TextBlock Text="{Binding MessageFull}" TextWrapping="Wrap" Margin="0,0,0,10"/>
                                            <TextBlock Text="{Binding File, StringFormat=Файл: {0}}" Foreground="{StaticResource TextSecondary}"/>
                                            <TextBlock Text="{Binding Function, StringFormat=Функция: {0}}" Foreground="{StaticResource TextSecondary}"/>
                                            <TextBlock Text="{Binding Line, StringFormat=Строка: {0}}" Foreground="{StaticResource TextSecondary}"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </DataGrid.RowDetailsTemplate>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="#" Binding="{Binding Id}" Width="SizeToCells" ElementStyle="{StaticResource LogTextStyle}"/>
                                <DataGridTextColumn Header="Время" Binding="{Binding Timestamp, StringFormat=HH:mm:ss.fff}" Width="SizeToCells" ElementStyle="{StaticResource LogTextStyle}"/>
                                <DataGridTextColumn Header="Тип" Binding="{Binding Level}" Width="SizeToCells" ElementStyle="{StaticResource LogTextStyle}"/>
                                <DataGridTextColumn Header="Категория" Binding="{Binding Category}" Width="SizeToCells" ElementStyle="{StaticResource LogTextStyle}"/>
                                <DataGridTextColumn Header="Текст" Binding="{Binding MessageSummary}" Width="*" ElementStyle="{StaticResource LogTextStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- Нижняя панель -->
                        <Grid Grid.Row="3" Margin="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                                <Button Content="Раскрыть всё" Command="{Binding DataContext.ExpandAllCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" />
                                <Button Content="Скрыть всё" Command="{Binding DataContext.CollapseAllCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" />
                            </StackPanel>
                            
                            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="Очистить" Command="{Binding DataContext.ClearCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" />
                                <Button Content="Копировать всё" Command="{Binding DataContext.CopyCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" />
                            </StackPanel>
                        </Grid>
                    </Grid>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>
    </Grid>
</Window>
