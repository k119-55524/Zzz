<Window x:Class="RemoteLogViewer.AddressManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление адресами" Height="300" Width="400" WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource WindowBackground}" Foreground="{StaticResource TextPrimary}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Название -->
            <TextBlock Text="Название (до 50)" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="3" Margin="2,0,2,0" FontSize="10" Foreground="{StaticResource TextSecondary}"/>
            <TextBox x:Name="TxtName" Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="3" MaxLength="50" Margin="2,0,2,10"/>

            <!-- IP и Порт -->
            <TextBlock Text="IP Адрес" Grid.Column="0" Grid.Row="2" Margin="2,0,2,0" FontSize="10" Foreground="{StaticResource TextSecondary}"/>
            <TextBox x:Name="TxtIp" Grid.Column="0" Grid.Row="3" Text="127.0.0.1" Margin="2,0,2,0"/>

            <TextBlock Text="Порт" Grid.Column="1" Grid.Row="2" Margin="2,0,2,0" FontSize="10" Foreground="{StaticResource TextSecondary}"/>
            <TextBox x:Name="TxtPort" Grid.Column="1" Grid.Row="3" Text="3030" Margin="2,0,2,0"/>

            <Button Content="Добавить" Grid.Column="2" Grid.Row="3" Click="BtnAdd_Click" Margin="6,0,2,0" VerticalAlignment="Bottom" Padding="10,4"/>
        </Grid>

        <ListBox x:Name="LstAddresses" Grid.Row="1" Background="{StaticResource PanelBackground}" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,10">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                        <Button x:Name="BtnDelete" Content="X" Grid.Column="1" Width="20" Height="20" Padding="0" Margin="5,0,0,0" Tag="{Binding}" Click="BtnDelete_Click" 
                                ToolTip="Удалить" Background="#AA3333" BorderThickness="0" Foreground="White" FontWeight="Bold"/>
                    </Grid>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsBuiltIn}" Value="True">
                            <Setter TargetName="BtnDelete" Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Закрыть" Width="80" Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>
