<ResourceDictionary x:Class="editor.Themes.DarkTheme"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Цвета оформления тёмной темы -->
    <Color x:Key="Color_Window_Background">#060B13</Color>
    <Color x:Key="Color_Panel_Background">#0D1929</Color>
    <Color x:Key="Color_Panel_Header">#112035</Color>
    <Color x:Key="Color_Border">#182E4B</Color>
    
    <Color x:Key="Color_Text_Primary">#E2C08D</Color>     <!-- Теплый песочно-желтый -->
    <Color x:Key="Color_Text_Secondary">#627A9C</Color>   <!-- Приглушенный сине-серый -->
    <Color x:Key="Color_Text_Light">#CDD9E5</Color>       <!-- Светлый бело-голубой для меню и логов -->
    <Color x:Key="Color_Accent">#388AF6</Color>           <!-- Премиальный ярко-синий -->
    <Color x:Key="Color_Accent_Hover">#50A4FF</Color>     <!-- Светящийся голубой -->
    <Color x:Key="Color_Hover">#1B3252</Color>
    <Color x:Key="Color_Checked">#1E3A63</Color>
    <Color x:Key="Color_Log_Even">#0A1321</Color>
    <Color x:Key="Color_Log_Odd">#0E1B2E</Color>
    
    <!-- Кисти -->
    <SolidColorBrush x:Key="Brush_Window_Background" Color="{StaticResource Color_Window_Background}" />
    <SolidColorBrush x:Key="Brush_Panel_Background" Color="{StaticResource Color_Panel_Background}" />
    <SolidColorBrush x:Key="Brush_Panel_Header" Color="{StaticResource Color_Panel_Header}" />
    <SolidColorBrush x:Key="Brush_Border" Color="{StaticResource Color_Border}" />
    
    <SolidColorBrush x:Key="Brush_Text_Primary" Color="{StaticResource Color_Text_Primary}" />
    <SolidColorBrush x:Key="Brush_Text_Secondary" Color="{StaticResource Color_Text_Secondary}" />
    <SolidColorBrush x:Key="Brush_Text_Light" Color="{StaticResource Color_Text_Light}" />
    <SolidColorBrush x:Key="Brush_Accent" Color="{StaticResource Color_Accent}" />
    <SolidColorBrush x:Key="Brush_Accent_Hover" Color="{StaticResource Color_Accent_Hover}" />
    <SolidColorBrush x:Key="Brush_Hover" Color="{StaticResource Color_Hover}" />
    <SolidColorBrush x:Key="Brush_Checked" Color="{StaticResource Color_Checked}" />
    <SolidColorBrush x:Key="Brush_Log_Even" Color="{StaticResource Color_Log_Even}" />
    <SolidColorBrush x:Key="Brush_Log_Odd" Color="{StaticResource Color_Log_Odd}" />

    <!-- Переопределение системных кистей для стилизации выделения (TreeView, ListView) -->
    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#1E3A63" />
    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="{StaticResource Color_Text_Primary}" />
    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#1B3252" />
    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="{StaticResource Color_Text_Primary}" />

    <!-- Базовый стиль для текста-заглушки в виджетах -->
    <Style x:Key="WidgetPlaceholderText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Brush_Text_Secondary}" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="TextAlignment" Value="Center" />
    </Style>

    <!-- Стилизация выпадающих меню под темную тему -->
    <Style TargetType="{x:Type Menu}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource Brush_Text_Primary}" />
    </Style>

    <Style TargetType="{x:Type MenuItem}">
        <Setter Property="Foreground" Value="{StaticResource Brush_Text_Primary}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type MenuItem}">
                    <Border x:Name="templateRoot" 
                            Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            SnapsToDevicePixels="True">
                        <Grid VerticalAlignment="Center">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="MenuItemIconColumnGroup"/>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="MenuHeaderColumnGroup"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="MenuItemGestureColumnGroup"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <Grid x:Name="IconContainer" Width="22" Margin="3" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <ContentPresenter x:Name="Icon" 
                                                  ContentSource="Icon" 
                                                  Width="16" Height="16" 
                                                  HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </Grid>
                            
                            <ContentPresenter x:Name="ContentPresenter" 
                                              Grid.Column="1" 
                                              ContentSource="Header" 
                                              RecognizesAccessKey="True" 
                                              Margin="6,4" 
                                              VerticalAlignment="Center" 
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            
                            <TextBlock x:Name="InputGestureText" 
                                       Grid.Column="3" 
                                       Text="{TemplateBinding InputGestureText}" 
                                       Foreground="{StaticResource Brush_Text_Secondary}" 
                                       Margin="10,2,10,2" 
                                       VerticalAlignment="Center"/>
                            
                            <Path x:Name="GlyphPanel" 
                                  Grid.Column="4" 
                                  Width="14"
                                  Data="M0,0 L4,4 L0,8 Z" 
                                  Fill="{StaticResource Brush_Text_Primary}" 
                                  VerticalAlignment="Center" 
                                  HorizontalAlignment="Center" 
                                  Visibility="Collapsed"/>
                            
                            <Popup x:Name="PART_Popup" 
                                   AllowsTransparency="True" 
                                   Focusable="False" 
                                   IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}" 
                                   PopupAnimation="{DynamicResource {x:Static SystemParameters.MenuPopupAnimationKey}}" 
                                   Placement="Bottom">
                                <Border x:Name="SubMenuBorder"
                                        Background="{DynamicResource Brush_Panel_Header}"
                                        BorderBrush="{DynamicResource Brush_Text_Primary}"
                                        BorderThickness="1"
                                        Padding="2">
                                    <ScrollViewer x:Name="SubMenuScrollViewer" 
                                                  CanContentScroll="True" 
                                                  VerticalScrollBarVisibility="Disabled" 
                                                  HorizontalScrollBarVisibility="Disabled">
                                        <ItemsPresenter x:Name="ItemsPresenter" 
                                                        KeyboardNavigation.DirectionalNavigation="Cycle" 
                                                        KeyboardNavigation.TabNavigation="Cycle" 
                                                        Grid.IsSharedSizeScope="True" 
                                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- Role triggers -->
                        <Trigger Property="Role" Value="TopLevelHeader">
                            <Setter TargetName="PART_Popup" Property="Placement" Value="Bottom"/>
                            <Setter TargetName="GlyphPanel" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="IconContainer" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="InputGestureText" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="Role" Value="TopLevelItem">
                            <Setter TargetName="PART_Popup" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="GlyphPanel" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="IconContainer" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="InputGestureText" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter TargetName="PART_Popup" Property="Placement" Value="Right"/>
                            <Setter TargetName="GlyphPanel" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="Role" Value="SubmenuItem">
                            <Setter TargetName="PART_Popup" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="GlyphPanel" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <!-- Highlight and mouse over triggers -->
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter Property="Background" Value="{DynamicResource Brush_Hover}"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource Brush_Hover}"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource Brush_Text_Secondary}"/>
                        </Trigger>
                        <!-- Ни один пункт меню в проекте не задаёт Icon/InputGestureText - схлопываем
                             зарезервированные под них колонки, иначе слева и справа от текста остаются
                             пустые поля (22px под иконку + отступы гесче-текста). -->
                        <Trigger Property="Icon" Value="{x:Null}">
                            <Setter TargetName="IconContainer" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="InputGestureText" Value="">
                            <Setter TargetName="InputGestureText" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="InputGestureText" Value="{x:Null}">
                            <Setter TargetName="InputGestureText" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Без переопределения Template нативный шаблон Separator игнорирует Background и рисует
         собственную рамку/тень. Стиль с x:Key (а не только TargetType) - неявное автоприменение по
         TargetType оказалось ненадёжным для literal <Separator/> внутри выпадающих меню (Popup):
         эмпирически проверено, что явная ссылка Style="{StaticResource ...}" всегда срабатывает,
         а неявная - не всегда. Поэтому ссылка на этот стиль проставлена явно на каждом <Separator/>
         в проекте (см. MainWindow.xaml, AssetsWidget.xaml). -->
    <Style x:Key="ThemedSeparatorStyle" TargetType="{x:Type Separator}">
        <Setter Property="Background" Value="{StaticResource Brush_Border}" />
        <Setter Property="Height" Value="1" />
        <Setter Property="Margin" Value="0,3" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Separator}">
                    <Border Height="{TemplateBinding Height}"
                            Background="{TemplateBinding Background}"
                            HorizontalAlignment="Stretch"
                            SnapsToDevicePixels="True"
                            UseLayoutRounding="True"
                            RenderOptions.EdgeMode="Aliased" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Кастомный стиль для CheckBox под темно-золотую тему -->
    <Style TargetType="{x:Type CheckBox}">
        <Setter Property="Foreground" Value="{DynamicResource Brush_Text_Primary}"/>
        <Setter Property="Background" Value="#060B13"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Brush_Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="6,0,0,0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CheckBox}">
                    <Grid x:Name="templateRoot" Background="Transparent" SnapsToDevicePixels="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border x:Name="checkBoxBorder" 
                                Width="14" Height="14"
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="2"
                                VerticalAlignment="Center">
                            <Grid x:Name="markGrid">
                                <Path x:Name="optionMark" 
                                      Data="M3,7 L5,9 L10,3" 
                                      Stroke="#060B13" 
                                      StrokeThickness="2" 
                                      Fill="Transparent"
                                      Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ContentPresenter x:Name="contentPresenter" 
                                          Grid.Column="1" 
                                          Focusable="False" 
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                          Margin="{TemplateBinding Padding}" 
                                          RecognizesAccessKey="True" 
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <!-- При выборе фон становится желтым (Brush_Text_Primary) -->
                            <Setter TargetName="checkBoxBorder" Property="Background" Value="{DynamicResource Brush_Text_Primary}"/>
                            <Setter TargetName="checkBoxBorder" Property="BorderBrush" Value="{DynamicResource Brush_Text_Primary}"/>
                            <Setter TargetName="optionMark" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="checkBoxBorder" Property="BorderBrush" Value="{DynamicResource Brush_Accent}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="checkBoxBorder" Property="Opacity" Value="0.5"/>
                            <Setter Property="Foreground" Value="{DynamicResource Brush_Text_Secondary}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!-- Стилизация контекстного меню. Пункты меню (MenuItem) внутри ContextMenu больше не
         переопределяются здесь отдельным дублирующим стилем - используется единый
         Style TargetType="{x:Type MenuItem}" выше, тот же, что и в верхнем меню (Файл/Правка/...),
         чтобы цвета и отступы не расходились и не дублировались по всей теме. -->
    <Style TargetType="{x:Type ContextMenu}">
        <Setter Property="Background" Value="{DynamicResource Brush_Panel_Header}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush_Text_Primary}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ContextMenu}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2"
                            Padding="1">
                        <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Единый тёмный скроллбар. Без x:Key - применяется неявно ко всем ScrollBar во всём приложении
         (Project, Assets, Console, Inspector и т.д.), без необходимости стилизовать каждый виджет отдельно. -->
    <Style x:Key="EditorScrollBarThumb" TargetType="{x:Type Thumb}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Border x:Name="ThumbBorder"
                            Background="{StaticResource Brush_Border}"
                            CornerRadius="3"
                            Margin="2" />
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ThumbBorder" Property="Background" Value="{StaticResource Brush_Text_Secondary}" />
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="ThumbBorder" Property="Background" Value="{StaticResource Brush_Accent}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="EditorScrollBarRepeatButton" TargetType="{x:Type RepeatButton}">
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border Background="Transparent" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ScrollBar}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="MinWidth" Value="12" />
        <Setter Property="MinHeight" Value="12" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollBar}">
                    <Grid Background="{TemplateBinding Background}">
                        <Track x:Name="PART_Track" IsDirectionReversed="True" Focusable="False">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Style="{StaticResource EditorScrollBarRepeatButton}" Command="ScrollBar.PageUpCommand" />
                            </Track.DecreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Style="{StaticResource EditorScrollBarThumb}" MinWidth="6" MinHeight="6" />
                            </Track.Thumb>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Style="{StaticResource EditorScrollBarRepeatButton}" Command="ScrollBar.PageDownCommand" />
                            </Track.IncreaseRepeatButton>
                        </Track>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!-- Для вертикального скроллбара -->
                        <Trigger Property="Orientation" Value="Vertical">
                            <Setter Property="Width" Value="12" />
                            <Setter Property="Height" Value="NaN" />
                        </Trigger>
                        <!-- Для горизонтального скроллбара -->
                        <Trigger Property="Orientation" Value="Horizontal">
                            <Setter Property="Width" Value="NaN" />
                            <Setter Property="Height" Value="12" />
                            <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False" />
                            <Setter TargetName="PART_Track" Property="Orientation" Value="Horizontal" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Общие стили модальных диалогов редактора. Добавляйте сюда новые контролы диалогов
         (TextBox/ComboBox/Button и т.п.), чтобы окна вроде NewScriptDialog/NewProjectDialog не
         заводили локальные копии визуального оформления и не расходились с общей тёмной темой. -->
    <Style x:Key="EditorDialogButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource Brush_Panel_Header}" />
        <Setter Property="Foreground" Value="{DynamicResource Brush_Text_Primary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush_Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="12,5" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource Brush_Hover}" />
                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource Brush_Accent}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource Brush_Checked}" />
                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource Brush_Accent}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="EditorDialogAccentButtonStyle" TargetType="Button" BasedOn="{StaticResource EditorDialogButtonStyle}">
        <Setter Property="Background" Value="{DynamicResource Brush_Accent}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush_Accent}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="EditorDialogHeaderCloseButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource Brush_Text_Primary}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Width" Value="45" />
        <Setter Property="Height" Value="30" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="#E81123" />
                            <Setter Property="Foreground" Value="White" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Правило обводок полей ввода (TextBox, ComboBox): 
         По умолчанию обводка должна быть Transparent (не белая, не синяя, не желтая). 
         Синяя обводка (Brush_Accent) появляется ТОЛЬКО при фокусе. -->
    <Style x:Key="EditorDialogTextBoxStyle" TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource Brush_Panel_Background}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="5,3" />
        <Setter Property="MinHeight" Value="24" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="SelectionBrush" Value="{DynamicResource Brush_Accent}" />
        <Setter Property="CaretBrush" Value="White" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="TextBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="1">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Margin="{TemplateBinding Padding}"
                                      Focusable="False"
                                      HorizontalScrollBarVisibility="Hidden"
                                      VerticalScrollBarVisibility="Hidden" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="TextBorder" Property="BorderBrush" Value="{DynamicResource Brush_Text_Primary}" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="TextBorder" Property="BorderBrush" Value="{DynamicResource Brush_Accent}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.6" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="EditorDialogComboBoxItemStyle" TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="ItemBorder"
                            Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource Brush_Hover}" />
                            <Setter Property="Foreground" Value="White" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource Brush_Checked}" />
                            <Setter Property="Foreground" Value="White" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource Brush_Text_Secondary}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="EditorDialogComboBoxEditableTextBoxStyle" TargetType="TextBox">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="6,2,4,2" />
        <Setter Property="SelectionBrush" Value="{DynamicResource Brush_Accent}" />
        <Setter Property="CaretBrush" Value="White" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <!-- Правило обводок полей ввода (TextBox, ComboBox): 
         По умолчанию обводка должна быть Transparent (не белая, не синяя, не желтая). 
         Синяя обводка (Brush_Accent) появляется ТОЛЬКО при фокусе. -->
    <Style x:Key="EditorDialogComboBoxStyle" TargetType="ComboBox">
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Background" Value="{DynamicResource Brush_Panel_Background}" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="4,2" />
        <Setter Property="MinHeight" Value="24" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource EditorDialogComboBoxItemStyle}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      Focusable="False"
                                      ClickMode="Press"
                                      Background="{TemplateBinding Background}"
                                      BorderBrush="{TemplateBinding BorderBrush}"
                                      BorderThickness="{TemplateBinding BorderThickness}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="ComboBorder"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="1">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="24" />
                                            </Grid.ColumnDefinitions>
                                            <ContentPresenter x:Name="ContentSite"
                                                              Grid.Column="0"
                                                              Margin="6,0,4,0"
                                                              VerticalAlignment="Center"
                                                              HorizontalAlignment="Left"
                                                              RecognizesAccessKey="True"
                                                              TextElement.Foreground="White"
                                                              Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource FindAncestor, AncestorType=ComboBox}}" />
                                            <Path Grid.Column="1"
                                                  Width="8"
                                                  Height="5"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Center"
                                                  Fill="{DynamicResource Brush_Text_Secondary}"
                                                  Data="M0,0 L4,5 L8,0 Z" />
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource FindAncestor, AncestorType=ComboBox}}" Value="True">
                                            <Setter TargetName="ComboBorder" Property="BorderBrush" Value="{DynamicResource Brush_Text_Primary}" />
                                        </DataTrigger>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="ComboBorder" Property="BorderBrush" Value="{DynamicResource Brush_Accent}" />
                                        </Trigger>
                                        <DataTrigger Binding="{Binding IsKeyboardFocusWithin, RelativeSource={RelativeSource FindAncestor, AncestorType=ComboBox}}" Value="True">
                                            <Setter TargetName="ComboBorder" Property="BorderBrush" Value="{DynamicResource Brush_Accent}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsEditable, RelativeSource={RelativeSource FindAncestor, AncestorType=ComboBox}}" Value="True">
                                            <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden" />
                                        </DataTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>

                        <TextBox x:Name="PART_EditableTextBox"
                                 Style="{StaticResource EditorDialogComboBoxEditableTextBoxStyle}"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Center"
                                 Margin="1,1,24,1"
                                 Visibility="Hidden"
                                 IsReadOnly="{TemplateBinding IsReadOnly}" />

                        <Popup x:Name="PART_Popup"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               Placement="Bottom"
                               PopupAnimation="Fade">
                            <Border Background="{DynamicResource Brush_Panel_Background}"
                                    BorderBrush="{DynamicResource Brush_Border}"
                                    BorderThickness="1"
                                    MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                <ScrollViewer MaxHeight="220" VerticalScrollBarVisibility="Auto">
                                    <ItemsPresenter />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.6" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Общий стиль поля поиска (изначально из Консоли) - скруглённое поле с кнопкой очистки,
         используется во всех виджетах с поиском (Console, Assets и т.д.), чтобы не дублировать разметку. -->
    <Style x:Key="Style_SearchTextBox" TargetType="TextBox">
        <Setter Property="Background" Value="#060B13" />
        <Setter Property="Foreground" Value="{DynamicResource Brush_Text_Primary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Brush_Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="20,4,22,4" />
        <Setter Property="Height" Value="26" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="SelectionBrush" Value="#555555" />
        <Setter Property="CaretBrush" Value="{DynamicResource Brush_Text_Primary}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="13">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <ScrollViewer x:Name="PART_ContentHost" Grid.Column="0" Focusable="False"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden" />

                            <Button x:Name="ClearButton" Grid.Column="1"
                                    Margin="0,0,6,0"
                                    Width="14" Height="14"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Cursor="Hand"
                                    Focusable="False"
                                    Visibility="Visible"
                                    Click="ClearSearch_Click">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="Transparent">
                                            <Path Data="M1,1 L11,11 M11,1 L1,11"
                                                  Stroke="{DynamicResource Brush_Text_Secondary}"
                                                  StrokeThickness="1.5"
                                                  Stretch="Uniform"
                                                  Margin="2"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Text" Value="">
                            <Setter TargetName="ClearButton" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="Text" Value="{x:Null}">
                            <Setter TargetName="ClearButton" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="#E2C08D" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="#E2C08D" />
                            <Setter Property="Background" Value="#0D1929" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
