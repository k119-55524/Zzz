<Window x:Class="editor.Views.AboutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="О программе" 
        Width="380" Height="180"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="False"
        Background="{DynamicResource Brush_Panel_Background}"
        BorderBrush="{DynamicResource Brush_Border}"
        BorderThickness="1"
        FontFamily="Roboto Mono">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/DarkTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="DialogButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="#2D2D30" />
                <Setter Property="BorderBrush" Value="{DynamicResource Brush_Border}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Foreground" Value="{DynamicResource Brush_Text_Primary}" />
                <Setter Property="Padding" Value="20,6" />
                <Setter Property="MinHeight" Value="28" />
                <Setter Property="MinWidth" Value="80" />
                <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" 
                                    Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="2"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter x:Name="contentPresenter" 
                                                  Focusable="False" 
                                                  HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center" 
                                                  Margin="{TemplateBinding Padding}" 
                                                  RecognizesAccessKey="True" 
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsDefault" Value="True">
                                    <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource Brush_Accent}" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#3F3F46" />
                                    <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource Brush_Accent_Hover}" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource Brush_Accent}" />
                                    <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource Brush_Accent}" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Background" Value="#2B2B2B" />
                                    <Setter TargetName="border" Property="BorderBrush" Value="#2B2B2B" />
                                    <Setter Property="Foreground" Value="{DynamicResource Brush_Text_Secondary}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border BorderBrush="{DynamicResource Brush_Text_Primary}" BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Текстовое описание по центру -->
            <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Редактор на основе ZzzEngine"
                           Foreground="{DynamicResource Brush_Text_Primary}"
                           FontSize="15" FontWeight="SemiBold"
                           HorizontalAlignment="Center" Margin="0,0,0,8" />
                <TextBlock Text="{Binding VersionText}"
                           Foreground="{DynamicResource Brush_Text_Secondary}"
                           FontSize="12"
                           HorizontalAlignment="Center" Margin="0,0,0,4" />
                <TextBlock Text="{Binding BuildDateText}"
                           Foreground="{DynamicResource Brush_Text_Secondary}"
                           FontSize="11"
                           HorizontalAlignment="Center" />
            </StackPanel>

            <!-- Отцентрированная кнопка ОК -->
            <Button Grid.Row="1" Content="ОК"
                    Style="{StaticResource DialogButtonStyle}"
                    HorizontalAlignment="Center"
                    Command="{Binding OkCommand}"
                    IsDefault="True" />
        </Grid>
    </Border>
</Window>
