<Window x:Class="editor.Views.ProjectValidationErrorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Проблемы при загрузке проекта" Height="400" Width="600"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="False"
        Background="{DynamicResource Brush_Window_Background}">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="30"
                      ResizeBorderThickness="0"
                      GlassFrameThickness="0"
                      CornerRadius="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- Стилизация кнопок диалога -->
        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2D2D30" />
            <Setter Property="Foreground" Value="{DynamicResource Brush_Text_Primary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Brush_Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="15,6" />
            <Setter Property="Margin" Value="5,0" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3E3E42" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#007ACC" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#007ACC" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogAccentButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogButtonStyle}">
            <Setter Property="Background" Value="#007ACC" />
            <Setter Property="BorderBrush" Value="#007ACC" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1C97EA" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#1C97EA" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#005A9E" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#005A9E" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Кнопка закрытия в шапке -->
        <Style x:Key="HeaderCloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{DynamicResource Brush_Text_Primary}" />
            <Setter Property="Width" Value="45" />
            <Setter Property="Height" Value="30" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E81123" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Заголовок окна -->
            <RowDefinition Height="*" />    <!-- Контент -->
            <RowDefinition Height="Auto" /> <!-- Кнопки внизу -->
        </Grid.RowDefinitions>

        <!-- 1. Кастомная шапка окна (TitleBar) -->
        <Border Grid.Row="0" Background="#2D2D30" BorderBrush="{DynamicResource Brush_Border}" BorderThickness="0,0,0,1" Height="30">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0">
                    <TextBlock Text="⚠️ Неполадки с файлами проекта" Foreground="{DynamicResource Brush_Text_Primary}" FontWeight="SemiBold" FontSize="12" />
                </StackPanel>
                <Button Grid.Column="1" Content="✕" Style="{StaticResource HeaderCloseButtonStyle}" Focusable="False" Click="Close_Click" WindowChrome.IsHitTestVisibleInChrome="True" />
            </Grid>
        </Border>

        <!-- 2. Основная рабочая область -->
        <Grid Grid.Row="1" Margin="20,15,20,15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Margin="0,0,0,10" TextWrapping="Wrap" 
                       Foreground="{DynamicResource Brush_Text_Primary}" FontSize="13">
                При проверке целостности проекта были обнаружены отсутствующие или поврежденные файлы конфигурации:
            </TextBlock>

            <Border Grid.Row="1" Background="#252526" BorderBrush="{DynamicResource Brush_Border}" BorderThickness="1" CornerRadius="2">
                <ListView x:Name="ErrorListView" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource Brush_Text_Primary}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Относительный путь" Width="200" DisplayMemberBinding="{Binding FilePath}" />
                            <GridViewColumn Header="Описание ошибки" Width="330" DisplayMemberBinding="{Binding ErrorMessage}" />
                        </GridView>
                    </ListView.View>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Foreground" Value="{DynamicResource Brush_Text_Primary}" />
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="Focusable" Value="False" />
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#2D2D30" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
            </Border>
        </Grid>

        <!-- 3. Кнопки управления -->
        <Border Grid.Row="2" Background="#2D2D30" BorderBrush="{DynamicResource Brush_Border}" BorderThickness="0,1,0,0" Padding="15,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" WindowChrome.IsHitTestVisibleInChrome="True">
                <Button Content="Прекратить загрузку" Style="{StaticResource DialogButtonStyle}" Click="Abort_Click" />
                <Button Content="Восстановить умолчания" Style="{StaticResource DialogAccentButtonStyle}" Click="RestoreDefaults_Click" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
