<UserControl x:Class="editor.Views.Widgets.GameWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:editor"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid Background="#1A1A1A">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <!-- Панель инструментов (Toolbar) -->
        <Border Grid.Row="0" Background="{DynamicResource Brush_Panel_Header}" Height="26" BorderBrush="{DynamicResource Brush_Border}" BorderThickness="0,0,0,1"
                IsEnabled="{Binding IsToolbarEnabled}">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0">
                <Button Content="😊" Width="20" Height="20" Foreground="{DynamicResource Brush_Text_Primary}" ToolTip="Game Toolbar Active">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="Transparent" CornerRadius="3">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Opacity" Value="0.4" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource Brush_Hover}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Border>

        <!-- Зона рендеринга -->
        <local:EngineViewHost Grid.Row="1" x:Name="ViewHost" ViewName="игры" />
    </Grid>
</UserControl>
