<UserControl x:Class="editor.Views.Widgets.WorldWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:editor"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#1A1A1A">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <!-- Панель инструментов (Toolbar) -->
        <Border Grid.Row="0" Background="#2D2D30" Height="26" BorderBrush="#3F3F46" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0">
                <!-- Будущие кнопки управления сценой/миром -->
            </StackPanel>
        </Border>

        <!-- Зона рендеринга -->
        <local:EngineViewHost Grid.Row="1" x:Name="ViewHost" />
    </Grid>
</UserControl>
