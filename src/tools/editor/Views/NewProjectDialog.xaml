<Window x:Class="editor.Views.NewProjectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="{DynamicResource Dialog_NewProject_Title}" Height="250" Width="500"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="False"
        Background="{DynamicResource Brush_Window_Background}"
        FontFamily="Roboto Mono">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="30"
                      ResizeBorderThickness="0"
                      GlassFrameThickness="0"
                      CornerRadius="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- Общие стили кнопок -->
        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2D2D30" />
            <Setter Property="Foreground" Value="{DynamicResource Brush_Text_Primary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Brush_Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,5" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3E3E42" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#007ACC" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#007ACC" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogAccentButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogButtonStyle}">
            <Setter Property="Background" Value="#007ACC" />
            <Setter Property="BorderBrush" Value="#007ACC" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1C97EA" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#1C97EA" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#005A9E" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#005A9E" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Кнопка закрытия в шапке -->
        <Style x:Key="HeaderCloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{DynamicResource Brush_Text_Primary}" />
            <Setter Property="Width" Value="45" />
            <Setter Property="Height" Value="30" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E81123" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стилизация текстового поля -->
        <Style x:Key="DialogTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#252526" />
            <Setter Property="Foreground" Value="{DynamicResource Brush_Text_Primary}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Brush_Border}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="5,3" />
            <Setter Property="SelectionBrush" Value="#007ACC" />
            <Setter Property="CaretBrush" Value="{DynamicResource Brush_Text_Primary}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="1">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#007ACC" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Шапка -->
            <RowDefinition Height="*" />    <!-- Поля ввода -->
            <RowDefinition Height="Auto" /> <!-- Кнопки -->
        </Grid.RowDefinitions>

        <!-- 1. Заголовок (TitleBar) -->
        <Border Grid.Row="0" Background="#2D2D30" BorderBrush="{DynamicResource Brush_Border}" BorderThickness="0,0,0,1" Height="30">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{DynamicResource Dialog_NewProject_Title}" Foreground="{DynamicResource Brush_Text_Primary}" FontWeight="SemiBold" FontSize="12" Margin="10,0" VerticalAlignment="Center" />
                <Button Grid.Column="1" Content="✕" Style="{StaticResource HeaderCloseButtonStyle}" Focusable="False" Click="Cancel_Click" WindowChrome.IsHitTestVisibleInChrome="True" />
            </Grid>
        </Border>

        <!-- 2. Форма ввода -->
        <Grid Grid.Row="1" Margin="20,15,20,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" /> <!-- Имя проекта -->
                <RowDefinition Height="10" />
                <RowDefinition Height="Auto" /> <!-- Путь -->
                <RowDefinition Height="*" />    <!-- Ошибка -->
            </Grid.RowDefinitions>

            <!-- Имя проекта -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{DynamicResource Dialog_NewProject_Name}" Foreground="{DynamicResource Brush_Text_Primary}" VerticalAlignment="Center" />
                <TextBox Grid.Column="1" x:Name="ProjectNameTextBox" Style="{StaticResource DialogTextBoxStyle}" TextChanged="ValidateInput" />
            </Grid>

            <!-- Расположение -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="80" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{DynamicResource Dialog_NewProject_Location}" Foreground="{DynamicResource Brush_Text_Primary}" VerticalAlignment="Center" />
                <TextBox Grid.Column="1" x:Name="ProjectPathTextBox" Style="{StaticResource DialogTextBoxStyle}" IsReadOnly="True" />
                <Button Grid.Column="2" Content="{DynamicResource Dialog_NewProject_Browse}" Margin="5,0,0,0" Style="{StaticResource DialogButtonStyle}" Click="Browse_Click" WindowChrome.IsHitTestVisibleInChrome="True" />
            </Grid>

            <!-- Сообщение об ошибке -->
            <TextBlock Grid.Row="3" x:Name="ErrorTextBlock" Foreground="#F25C5C" TextWrapping="Wrap" Margin="0,10,0,0" VerticalAlignment="Top" Visibility="Collapsed" />
        </Grid>

        <!-- 3. Кнопки управления -->
        <Border Grid.Row="2" Background="#2D2D30" BorderBrush="{DynamicResource Brush_Border}" BorderThickness="0,1,0,0" Padding="15,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" WindowChrome.IsHitTestVisibleInChrome="True">
                <Button Content="{DynamicResource Dialog_NewProject_Create}" x:Name="CreateButton" Style="{StaticResource DialogAccentButtonStyle}" Click="Create_Click" Margin="0,0,8,0" IsEnabled="False" />
                <Button Content="{DynamicResource Dialog_NewProject_Cancel}" Style="{StaticResource DialogButtonStyle}" Click="Cancel_Click" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
