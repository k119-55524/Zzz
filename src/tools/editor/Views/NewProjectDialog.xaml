<Window x:Class="editor.Views.NewProjectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="{DynamicResource Dialog_NewProject_Title}" Height="250" Width="500"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="False"
        Background="{DynamicResource Brush_Window_Background}"
        BorderBrush="{DynamicResource Brush_Text_Primary}"
        BorderThickness="1"
        FontFamily="Roboto Mono">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="30"
                      ResizeBorderThickness="0"
                      GlassFrameThickness="0"
                      CornerRadius="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/DarkTheme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <!-- Шапка -->
            <RowDefinition Height="*" />    <!-- Поля ввода -->
            <RowDefinition Height="Auto" /> <!-- Кнопки -->
        </Grid.RowDefinitions>

        <!-- 1. Заголовок (TitleBar) -->
        <Border Grid.Row="0" Background="{DynamicResource Brush_Panel_Header}" BorderBrush="{DynamicResource Brush_Border}" BorderThickness="0,0,0,1" Height="30">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{DynamicResource Dialog_NewProject_Title}" Foreground="{DynamicResource Brush_Text_Primary}" FontWeight="SemiBold" FontSize="12" Margin="10,0" VerticalAlignment="Center" />
                <Button Grid.Column="1" Content="✕" Style="{StaticResource EditorDialogHeaderCloseButtonStyle}" Focusable="False" Click="Cancel_Click" WindowChrome.IsHitTestVisibleInChrome="True" />
            </Grid>
        </Border>

        <!-- 2. Форма ввода -->
        <Grid Grid.Row="1" Margin="20,15,20,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" /> <!-- Имя проекта -->
                <RowDefinition Height="10" />
                <RowDefinition Height="Auto" /> <!-- Путь -->
                <RowDefinition Height="*" />    <!-- Ошибка -->
            </Grid.RowDefinitions>

            <!-- Имя проекта -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{DynamicResource Dialog_NewProject_Name}" Foreground="{DynamicResource Brush_Text_Primary}" VerticalAlignment="Center" />
                <TextBox Grid.Column="1" x:Name="ProjectNameTextBox" Style="{StaticResource EditorDialogTextBoxStyle}" TextChanged="ValidateInput" />
            </Grid>

            <!-- Расположение -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="80" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{DynamicResource Dialog_NewProject_Location}" Foreground="{DynamicResource Brush_Text_Primary}" VerticalAlignment="Center" />
                <TextBox Grid.Column="1" x:Name="ProjectPathTextBox" Style="{StaticResource EditorDialogTextBoxStyle}" IsReadOnly="True" />
                <Button Grid.Column="2" Content="{DynamicResource Dialog_NewProject_Browse}" Margin="5,0,0,0" Style="{StaticResource EditorDialogButtonStyle}" Click="Browse_Click" WindowChrome.IsHitTestVisibleInChrome="True" />
            </Grid>

            <!-- Сообщение об ошибке -->
            <TextBlock Grid.Row="3" x:Name="ErrorTextBlock" Foreground="#F25C5C" TextWrapping="Wrap" Margin="0,10,0,0" VerticalAlignment="Top" Visibility="Collapsed" />
        </Grid>

        <!-- 3. Кнопки управления -->
        <Border Grid.Row="2" Background="{DynamicResource Brush_Panel_Header}" BorderBrush="{DynamicResource Brush_Border}" BorderThickness="0,1,0,0" Padding="15,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" WindowChrome.IsHitTestVisibleInChrome="True">
                <Button Content="{DynamicResource Dialog_NewProject_Create}" x:Name="CreateButton" Style="{StaticResource EditorDialogAccentButtonStyle}" Click="Create_Click" Margin="0,0,8,0" IsEnabled="False" />
                <Button Content="{DynamicResource Dialog_NewProject_Cancel}" Style="{StaticResource EditorDialogButtonStyle}" Click="Cancel_Click" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
