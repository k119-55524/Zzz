# Генеральный план разработки: Вращающийся текстурированный 3D-куб (Textured Spinning Cube)

---

## 📜 Правила ведения и исполнения плана (Обязательно к соблюдению)

1. **Никаких правок кода без явной команды:** Запрещено создавать или изменять файлы исходного кода до получения прямой команды от пользователя. Сначала каждый шаг и архитектурное решение подробно обсуждаются.
2. **Единый стандарт системы координат (Left-Handed Standard):** В движке принят единый стандарт — **Левосторонняя система координат (Y-up, Z-forward, диапазон глубины NDC Z: $[0, 1]$)**. Компенсация инвертированной оси Y в Vulkan выполняется на аппаратном уровне GPU через отрицательную высоту вьюпорта (`viewport.y = height; viewport.height = -height;`), поэтому матрицы на CPU рассчитываются один раз и одинаковы для DirectX 12, Vulkan и Metal.
3. **Критерий готовности шага (Definition of Done — DoD):**
   - Код написан строго по ТЗ текущего этапа (`stage_XX_...md`);
   - Для всех подходящих логических, математических и структурных модулей написаны и **успешно пройдены юнит-тесты** (Google Test / CTest); графический вывод проверяется на этапе рендер-пайплайна;
   - Проект компилируется начисто без ошибок;
   - Пользователь явно подтвердил: *«Шаг принят»*;
   - Только после этого статус в `general_plan.md` меняется на `✅ Выполнено`, и формируется план следующего шага.
4. **Фиксация точек отката (Git Commit):** После успешного завершения и утверждения каждого шага фиксируется атомарный Git-коммит (например, `git commit -m "feat(math): completed stage 01 - Vec2, Vec3, Vec4"`), обеспечивающий мгновенный безопасный откат при необходимости.
5. **Строгая изоляция платформенного кода (Zero Platform Leaks):** В кроссплатформенных модулях (`src/math/`, `src/gui/`, `src/engine/`) категорически запрещено прямое подключение платформозависимых заголовков (`<windows.h>`, `<d3d12.h>`, `<vulkan/vulkan.h>`, `<Metal/Metal.h>`). Вся специфика строго изолирована в `src/engine/platforms/` и `src/engine/gapi/`.
6. **Стратегия обработки ошибок и производительности:**
   - **Контрактные проверки разработки (`ensure` по надобности):** Для валидации инвариантов классов, корректности аргументов, не-null указателей и состояний готовности подсистем (например, обращение к путям пользователя после `InitializeUserData`) вместо `assert` или тяжелых исключений используется встроенный `ensure(condition, message)` из `src/core/utils/Ensure.h` (работает в Debug/Dev через `source_location`, в Release компилируется в zero-overhead no-op);
   - **Горячие циклы кадра (`Update`, `PrepareFrame`, `Submit`):** Никаких тяжелых C++ исключений (`throw`), только быстрые коды возврата, `ensure` и логгер;
   - **Восстанавливаемые ошибки ввода-вывода (VFS, загрузка ресурсов):** Использование `std::expected<T, eErrorCode>` и кодов ошибок.
7. **Регламент изменений спецификаций на лету:** Если в процессе реализации шага возникает необходимость изменить сигнатуру или расширить структуру — изменение сначала обсуждается и согласуется в чате, вносится в `stage_XX_...md`, и только затем пишется код.
8. **Единый источник правды (Single Source of Truth):** Все константы, форматы мета-файлов (`.meta`), структуры сериализации данных, валидация путей и упаковка в `package.dat` реализуются **строго на C++ через нативную DLL** (`studio_core_dll` / `assets_builder_dll`). C# WPF интерфейс студии проектов служит исключительно графической оболочкой (UI) и вызывает нативный C-API через P/Invoke.
9. **Модульность архитектуры библиотек:** 
   - Низкоуровневые модули (`core_lib`, `math_lib`, `gapi_lib`, `gui_lib`) независимы и могут использоваться автономно для утилит и легковесных приложений;
   - **`engine_lib` остается главной высокоуровневой библиотекой для игр**, транзитивно объединяющей (через `PUBLIC` CMake-линковку) все подсистемы: игры подключают только `engine_lib` и сразу получают сцены, ZzzGUI, рендер и скрипты.
10. **Строгая топологическая сортировка (Topological Dependency Order):** Каждый пункт в плане расположен так, что на момент его реализации **все необходимые ему классы, структуры, форматы данных и подсистемы уже созданы и протестированы на предыдущих шагах**.
11. **Учет будущих потребностей (Forward Compatibility):** При проектировании и реализации текущего пункта необходимо явно учитывать требования **последующих** пунктов плана (сигнатуры функций, форматы структур/сериализации, точки расширения API), чтобы избежать ломающих переделок уже готового и протестированного кода на будущих этапах. Это не означает реализацию будущего функционала заранее (см. правило 15) — только такое проектирование текущего пункта, чтобы будущие этапы могли встроиться в него без рефакторинга.
12. **Data-Oriented Design (DOD), предотвращение False Sharing и Zero-Allocation в горячем цикле кадра:** Горячий конвейер рендеринга, скриптов и обновления компонентов проектируется по принципам DOD — данные хранятся в непрерывных массивах для максимального L1/L2 Cache Hit, скрипты группируются в пулы по типам для невиртуального пакетного обновления, очереди отрисовки используют линейные арена-аллокаторы кадра (Frame Arena), а межпоточные структуры (очереди кадров $N / N-1$, `DoubleBufferedVector`, кольцевые буферы и атомарные счётчики пула задач) выравниваются через `alignas(std::hardware_destructive_interference_size)` (64 байта) для гарантированного исключения False Sharing между ядрами CPU.
13. **Именование файлов этапов:** Файл текущего шага всегда именуется по строгому шаблону `stage_<номер>_<имя>.md` (например, `stage_01_vectors.md`).
14. **Единый источник истины о текущей работе:** Файл текущего шага содержит подробную техническую спецификацию, сигнатуры функций, форматы структур и чек-лист **только для текущего активного пункта**.
15. **Отсутствие предварительных файлов:** Для невыполненных (будущих) пунктов отдельные файлы планов **не создаются заранее**.
16. **Создание конкретных планов строго по команде:** Детальный план следующего этапа (например, `stage_02_mat4.md`) создается **только по прямой команде пользователя** после завершения, валидации и утверждения предыдущего пункта.
17. **Гибкость и адаптация:** По мере реализации любого пункта в генеральный план могут вноситься корректировки, дополнения или уточнения архитектуры при необходимости.
18. **Прозрачность для любого разработчика:** Любой читатель плана в любой момент времени гарантированно видит:
   - **Куда идем:** общая финальная цель и сквозной список этапов;
   - **Что сделано:** пункты со статусом `✅ Выполнено`;
   - **Что делаем прямо сейчас:** пункт со статусом `🔄 В работе` и прямая ссылка на соответствующий `stage_XX_...md`.
19. **Итоговая документация и завершение:** После выполнения всех пунктов составляется полная документация в `docs/` на основе накопленных файлов этапов, после чего вся папка `current_plan/` удаляется.
20. **Правило кроссплатформенной проверки на контрольных точках:** пункт помечается `✅ Выполнено` только после факта сборки и реального прогона на платформах, актуальных для текущей стадии платформенного роллаута (см. раздел «Платформенный роллаут» ниже: сначала Windows/DirectX12+Vulkan, затем Linux/Vulkan, затем macOS/мобильные/Metal). Результат прогона фиксируется в файле этапа.
21. **Правило "технология не фиксируется заранее":** для возможностей, конкретная реализация которых не критична прямо сейчас (шрифты, блюр/частицы, продвинутое сжатие ресурсов и т.п.), генеральный план фиксирует ЧТО нужно (выбор технологии, способ отрисовки, обёртки API), а не КАК именно — конкретная технология выбирается и фиксируется в файле этапа непосредственно перед реализацией, не на этапе генерального планирования.
22. **Правило сверки с существующим кодом перед стартом пункта:** перед началом работы над любым пунктом — быстрый поиск по кодовой базе, не реализовано ли уже частично нужное (например: `RenderManager`/`PackageManager`/`ScriptFactory`/`tools/editor` уже существовали на момент составления этого плана). Решение "расширяем существующее" vs "переписываем" явно фиксируется в файле этапа.
23. **Правило версионирования форматов:** при изменении форматов сериализации (`package.dat`, `.meta`, `.zmat`, `user.dat` и т.п.) обязателен явный план миграции/совместимости, чтобы старые файлы не ломались молча.
24. **Правило единого тестового проекта:** `zzz_assets_test_000` — постоянное хранилище тестовых ассетов и сцен для QA; тестовые ассеты не создаются одноразово и не удаляются после проверки пункта, если явно не решено иное пользователем.
25. **Правило именования "своего WPF":** кроссплатформенный UI-фреймворк движка временно называется `ZzzGUI` в этом документе — окончательное имя выбирается пользователем отдельно (см. обсуждение в чате) и после выбора заменяется по всему плану одним проходом.
26. **Правило разделения POD-структур данных и сервисных классов (Value Objects vs Stateful Objects):**
    - **Математика и дескрипторы данных (`src/math/`, `Vertex3D`, `Viewport`):** поля **открытые (`public`)**, структуры являются `Standard Layout POD`, чистые данные без скрытого состояния (`pt.x`, `sz.width`, `v.z`, `rect.position`);
    - **Подсистемы, менеджеры, компоненты и контролы (`RenderManager`, `Camera`, `Transform`, `UIElement`):** **строгая инкапсуляция (поля `private` / `protected`)**, доступ только через методы с валидацией инвариантов и контролем жизненного цикла.
27. **Правило структуры и конфигурации юнит-тестов (QA Subfolders & Centralized Toggling):**
    - Файлы тестов раскладываются строго по тематическим подпапкам модуля (`src/qa/tests/math/`, `src/qa/tests/core/`, `src/qa/tests/gapi/`, и т.д.);
    - Включение и отключение наборов тестов управляется централизованно через единый файл **`src/qa/tests/TestsConfig.h`** путем комментирования/раскомментирования директив `#define Z_TEST_...` без ручного редактирования или удаления файлов тестов.
28. **Правило ограничения на новые типы и алиасы (Type Sprawl Control):** движок уже содержит большое количество типов (`Vec2/3/4`, `Mat3/4`, `Quat`, `Color3/4`, перечисления `eResourceType`/`ePixelFormat`/`eIndexFormat`/`eVertexSemantic` и т.д.), поэтому появление новых типов, обёрток и type-алиасов (в частности коротких алиасов вида `XxxF`/`XxxD`/`XxxU8`) **не выполняется по умолчанию «для единообразия» или «на будущее»** — каждый такой алиас/тип явно обсуждается и согласовывается с пользователем в чате и только затем фиксируется в файле текущего этапа. Отсутствие алиаса — не повод добавлять его в одностороннем порядке, если это явно не согласовано.
29. **Правило платформенной специализации через Compile-Time Type Aliases (Zero `#ifdef` Spaghetti):** Для платформозависимых подсистем (`Input`, `MainLoop`, `FileSystem`, структуры конфигураций) запрещено размазывать директивы `#ifdef` внутри тел методов. Архитектура строится по единому стандарту движка:
    - Базовый класс (`XxxBase`) содержит всю общую логику (на чистом C++23, 0 дефайнов внутри методов);
    - Платформенные классы (`platforms/XxxPlatform`) инкапсулируют нативные API платформ в отдельных изолированных файлах. **Важно:** если между десктопными ОС нет различий в используемых API (как в случае со стандартным дисковым вводом-выводом C++23 в `FileSystem`), Windows, Linux и macOS **сводятся в один класс `XxxDesktop`** без дублирования кода. Разделение на отдельные `XxxMSWin`, `XxxLinux`, `XxxMacOS` выполняется **только при наличии реальной специфики нативных API ОС** (например, события окон и ввод Win32 vs Wayland vs Cocoa в `Input` и `MainLoop`);
    - Для мобильных ОС (`platforms/XxxAndroid`, `platforms/XxxiOS`) создаются изолированные специализации под нативные SDK (NDK `AAssetManager`, Apple `NSBundle`);
    - Центральный заголовочный файл (`Xxx.h`) содержит единый блок `#if Z_...` и экспортирует тип через `using Xxx = XxxPlatform;` (или `XxxDesktop`).
    Это гарантирует Zero-Cost Abstraction (без `vtable` и лишних аллокаций памяти) и полную изоляцию нативных SDK (Win32, Android NDK, Cocoa).
30. **Правило минимизации оверинжиниринга и соблюдения принципа YAGNI (You Aren't Gonna Need It):**
    Категорически запрещено реализовывать методы, интерфейсы, вспомогательные функции, обёртки и геттеры «про запас», «на будущее» или «для полноты API», если у них нет конкретного реального потребителя прямо сейчас в кодовой базе движка:
    - **Публичный контракт классов:** должен содержать строго минимальный набор методов, необходимый для выполнения текущих задач движка (например, в `FileSystem` оставлены только 4 фактически используемых метода: `FileExists`, `ReadBytes`, `ReadAllBytes`, `WriteAllBytes`, а неиспользуемые текстовые обёртки `ReadAllText`/`WriteAllText`/`AppendText` и `GetFileSize` удалены);
    - **Герметичность и защита от утечек:** запрещено добавлять «удобные» геттеры к внутренним подсистемам (такие как `GetPath()` или `GetNativeData()`), если они никем не вызываются или пробивают инкапсуляцию подсистемы;
    - **Тесты как зеркало контракта:** тесты не должны служить оправданием существования мертвого кода (код пишется для работы движка, а не ради тестов). Если метод не нужен движку — он подлежит удалению;
    - Расширение функциональности классов выполняется строго по принципу Just-In-Time — в тот момент, когда в новом методе возникает объективная необходимость следующего этапа разработки.
31. **Правило синтаксиса платформенных проверок (`#if defined(...)` Standard):**
    Для всех проверок платформ и графических API в директивах препроцессора используется строго синтаксис `#if defined(...)` (например, `#if defined(Z_WINDOWS)`, `#if defined(Z_LINUX)`, `#if defined(Z_ANDROID)`, `#if defined(Z_MACOS)`, `#if defined(Z_IOS)`, `#if defined(Z_EDITOR)`, `#if defined(Z_DESKTOP)`, `#if defined(Z_MOBILE)`, `#if defined(Z_D3D12)`, `#if defined(Z_VULKAN)`, `#if defined(Z_METAL)`). Это исключает предупреждения компиляторов `-Wundef` при обращении к необъявленным макросам на других ОС и обеспечивает единообразие во всей кодовой базе.
32. **Правило тотальной изоляции платформенных файлов (Full-File Platform Guards):**
    Любой платформозависимый файл (`.h`, `.cpp`, `.mm`) оборачивается в блок препроцессора `#if defined(...)` **полностью и целиком**:
    - В заголовочных файлах (`.h`): сразу после `#pragma once` и `#include "core/utils/Defines.h"` ставится `#if defined(Z_PLATFORM)`, обрамляя все нативные системные инклюды, структуры и объявления классов до конца файла (`#endif // defined(Z_PLATFORM)`);
    - В файлах реализации (`.cpp`, `.mm`): сразу после `#include "core/utils/Defines.h"` ставится `#if defined(Z_PLATFORM)`, обрамляя всё тело файла (`#endif // defined(Z_PLATFORM)`).
    Благодаря этому при сборке на нецелевой платформе файл превращается в пустую единицу трансляции (0 байт), ни один сторонний SDK-заголовок (Win32, Wayland, Android NDK, Apple Cocoa/UIKit) не попадает в парсер компилятора, и исключаются любые конфликты линковки.
    *Исключение:* платформенные DTO-структуры сериализации данных пакета (`ViewData*.h`, `ProjectPlatformData*.h`), так как десктопная утилита упаковщика `PackagePacker` должна сериализовать манифесты для всех целевых платформ.

---

## 📚 Референсный проект с примерами реализации
> **Путь к референсному проекту:** [`c:\Workspaces\DZzz`](file:///c:/Workspaces/DZzz)  
> Содержит примеры шаблонного буфера вершин (`CPUVertexBuffer.cppm`), маппера форматов (`VertexFormatMapper.cppm`) и другие реализации подсистем Zzz.

---

## 🎯 Финальная цель проекта
Обеспечить полный цикл работы графической и ресурсной подсистем ZzzEngine: от добавления исходных ассетов в проект до отображения и **вращения текстурированного 3D-куба пользовательским скриптом** в окне приложения через кроссплатформенный GAPI (DirectX 12 / Vulkan / Metal).  
Рендеринг работает по строгой многопоточной схеме кадрового конвейера: **в потоке $N$ подготавливаются данные сцены и записываются команды, в потоке $N-1$ команды исполняются на GPU и выводятся на экран**.

> **💡 Главная миссия Special GUI Layer (ZzzGUI в `gui_lib`):** Создание высокопроизводительного автономного MVVM UI-фреймворка на нативном GAPI с возможностью прямого бесшовного встраивания интерактивных **3D объектов и GPU-эффектов (частицы, свечение, Glass/Acrylic блюр) прямо в элементы интерфейса** с On-Demand рендерингом десктопного уровня.


## 🧭 Приоритеты MVP (уточнено пользователем, 2026-08-31)

1. **Главный критерий приёмки любого пункта — идентичное поведение сборки на всех целевых платформах.** Не «скомпилировалось на Windows», а «прогнано и одинаково выглядит/работает» (см. новые контрольные точки кроссплатформенной проверки в таблице ниже).
2. **ZzzGUI (кроссплатформенный WPF-подобный UI-фреймворк) важнее полноты 3D-графики.** При конфликте приоритетов между «доделать графический эффект» и «довести GUI до одинаковой работы на всех платформах» — приоритет у GUI.
3. **Графика на первом проходе — минимально достаточная, а не максимально красивая.** Технологии, не критичные прямо сейчас (конкретная реализация шрифтов, блюр/частицы, переходы сцен), не фиксируются заранее — решаются в момент подхода к пункту (правило 21). Ничего не исключается из плана заранее без явного решения пользователя.

## 🗺️ Платформенный роллаут (уточнено пользователем, 2026-08-31)

Платформы вводятся по очереди, не все сразу — со своими контрольными точками (КП) в таблице ниже:

1. **Windows** (DirectX12 + Vulkan) — основная платформа разработки; все КП-1..КП-3 проверяются здесь.
2. **Linux** (Vulkan) — следующая цель сразу после подтверждения куба и ZzzGUI-MVP на Windows (КП-4).
3. **macOS и мобильные (Android/iOS, Metal)** — позже (КП-5). *Предположение: macOS сгруппирован с мобильными платформами, т.к. явно не назван пользователем отдельно и разделяет бэкенд Metal с iOS — уточнить при необходимости.*

---

## 📌 Текущее состояние разработки

> [!IMPORTANT]
> **Текущий активный пункт:** `Пункт 7. Шаблонная система буфера вершин и маппер GAPI`  
> **Статус:** ⏳ Не начато  
> **Файл детального плана текущего шага:** Ожидает формирования (по команде)

---

## 📋 Сквозная таблица этапов реализации (Строгая последовательность зависимостей)

### Уровень 1: Математический, файловый и платформенный фундамент
| № | Этап разработки | Статус | Файл этапа | Краткое описание |
|---|---|---|---|---|
| **1** | Векторная математика (`Vec2`, `Vec3`, `Vec4`) и унификация `Point2D` | ✅ Выполнено | [`stage_01_vectors.md`](stage_01_vectors.md) | Шаблоны (`zF32`/`zI32`/`zU32`), C++23 концепты, операции, открытые x/y в `Point2D`, сериализация, `std::formatter` |
| **2** | Матрицы `Mat4` и `Mat3` (базовые операции, аффинные трансформации, базис и проекции) | ✅ Выполнено | [`stage_02_mat4.md`](stage_02_mat4.md) | Умножение ($v \cdot M$), $TRS$ ($Z \to X \to Y$), `LookAtLH`, `PerspectiveFovLH` $[0, 1]$, `Mat3` базис и матрица нормалей, сериализация |
| **3** | Кватернионы `Quat` | ✅ Выполнено | [`stage_03_quat.md`](stage_03_quat.md) | 3D вращения без Gimbal Lock, `Slerp`, `FromEuler`/`ToEuler`, `ToMat4`, сериализация |
| **4** | Базовые перечисления (Enums) и структуры ядра | ✅ Выполнено | [`stage_04_enums_and_core_structs.md`](stage_04_enums_and_core_structs.md) | `eResourceType`, `ePixelFormat`, `eIndexFormat`, `ConverterGAPITypes`, `eVertexSemantic`, `Vertex3D`, `AttributeRange`, выравнивание CBV (256б), `EnumToString` |
| **5** | Кроссплатформенная файловая система (`FileSystem`, `eFileLocation`) | ✅ Выполнено | [`stage_05_filesystem.md`](stage_05_filesystem.md) | Compile-Time Type Alias (`using FileSystem = ...`), `FileSystemBase`, `FileSystemDesktop`, `eFileLocation`, ranged-read, изоляция `Path` |
| **6** | Рефакторинг подсистемы аппаратуры (`HardwareManager`, `HardwareState`) | ✅ Выполнено | [`stage_06_hardware_manager.md`](stage_06_hardware_manager.md) | Вынос сбора телеметрии (CPU, RAM, GPU, Storage, Network) из монолитного `Platform::GatherHardwareState` в чистый сервис по Правилу 29 (SRP, изоляция нативных SDK Win32 DXGI/SMBIOS) |

### Уровень 2: GAPI-ресурсы, содержимое куба и сквозной рендер
| № | Этап разработки | Статус | Файл этапа | Краткое описание |
|---|---|---|---|---|
| **7** | Шаблонная система буфера вершин и маппер GAPI | ⏳ Не начато | — | `CPUVertexBuffer<Attrs...>` и `VertexFormatMapper` (референс: `c:\Workspaces\DZzz\...`) |
| **8** | Кроссплатформенные абстракции ресурсов GAPI | ⏳ Не начато | — | `IVertexBuffer`, `IIndexBuffer`, `IConstantBuffer` (двойная буферизация кадра), `ITexture2D` |
| **9** | Управление зоной рендеринга и DPI-масштабирование | ⏳ Не начато | — | Viewport, Scissor, Safe Area, логические единицы (DIP / Canvas Units), компенсация Y в Vulkan |
| **10** | Компонент `Transform` | ⏳ Не начато | — | Позиция, вращение `Quat`, масштаб, иерархия parent/child, кэш World-матрицы |
| **11** | Компонент `Camera` | ⏳ Не начато | — | Углы обзора, View/Projection матрицы, автоматический пересчет Aspect при ресайзе |
| **12** | Структура `Mesh` и генератор 3D-куба | ⏳ Не начато | — | Единичный куб: 6 граней $\times$ 4 вершины = 24 вершины (нормали, UV), 36 индексов |
| **13** | Класс `Material` и формат `.zmat` | ⏳ Не начато | — | Динамический Property Bag (`SetColor`, `SetFloat`, `SetTexture`), файл материала |
| **14** | Базовый текстурный шейдер `BasicTextured.hlsl` | ⏳ Не начато | — | HLSL шейдер с текстурированием и простым рассеянным освещением |
| **15** | Компонент `MeshRenderer` | ⏳ Не начато | — | Связка GUID меша и GUID материала на `GameObject` |
| **16** | Отрисовка куба на DX12/Vulkan/Metal **(частично реализовано)** | ⏳ Не начато | — | Доведение `SurfView_DX/VK/Metal` и `RenderManager` до реальной отрисовки текстурированного куба |
| **17** | Компиляция шейдеров и кэширование PSO | ⏳ Не начато | — | Сборка через `dxc.exe` в DXIL и SPIR-V, `VkPipelineCache` и `ID3D12PipelineLibrary` |
| **18** | 🚦 КП-1: кроссплатформенная проверка статичного куба | ⏳ Не начато | — | Сборка и проверка отрисовки куба на Windows (DirectX12 + Vulkan) |

### Уровень 3: Скриптование и первая живая проверка вращения
| № | Этап разработки | Статус | Файл этапа | Краткое описание |
|---|---|---|---|---|
| **19** | Механизм скриптования `GameObject` по GUID **(частично реализовано)** | ⏳ Не начато | — | Доведение существующей `ScriptFactory`/`ScriptRegistry`/`ScriptStorage` под Transform/Mesh/Material |
| **20** | Фасады движка для скриптов (Engine Facades) | ⏳ Не начато | — | Безопасные обертки API (`EngineFacade`, `SceneFacade`, `ViewFacade`) с разграничением прав |
| **21** | Изоляция и контроль видимости скриптов | ⏳ Не начато | — | Виртуальные методы ограничения доступа к скриптам верхнего уровня для нижележащих скриптов |
| **22** | Вращение куба пользовательским скриптом | ⏳ Не начато | — | Скрипт `CubeRotatorScript` на объекте куба, вращение во времени (`DeltaTime`) |
| **23** | 🚦 КП-2: кроссплатформенная проверка вращающегося куба | ⏳ Не начато | — | Тестирование параллельной схемы кадрового конвейера ($N$ готовит / $N-1$ рендерит) на Windows (DX12 + VK) |
| **24** | Сериализуемые поля и ссылки скриптов (`FieldRef`) | ⏳ Не начато | — | Unity-like поля в скриптах: ссылки на `GameObject`, другие скрипты и ассеты с 2-фазной линковкой |

### Уровень 4: Слои сцены и пространственные контейнеры
| № | Этап разработки | Статус | Файл этапа | Краткое описание |
|---|---|---|---|---|
| **25** | Настраиваемые структуры хранения объектов слоя | ⏳ Не начато | — | Ручной выбор контейнера слоя (`ISceneStorage`): AABB-дерево, линейный ("всё подряд"), UI Hierarchy |
| **26** | Архитектура слоев сцены и регулировка частоты кадров | ⏳ Не начато | — | `SceneLayer3D`, `SceneLayerGUI`, `SceneLayerSpecialGUI` (интеграция `gui_lib`, 3D в UI, независимый FPS, On-Demand) |

### Уровень 5: Ввод и UI-фреймворк ZzzGUI
| № | Этап разработки | Статус | Файл этапа | Краткое описание |
|---|---|---|---|---|
| **27** | Автоматическая маршрутизация ввода в слои сцены | ⏳ Не начато | — | Доставка Тач / Мышь / Клавиатура / Геймпад: Hit-Testing в GUI с поглощением кликов и проброс в 3D World |
| **28** | Движок разметки ZzzGUI (`Measure`/`Arrange`) | ⏳ Не начато | — | CMake-таргет `gui_lib`: двухпроходный layout, контейнеры `StackPanel`, `Grid`, `Canvas`, `ScrollViewer`, `Border` |
| **29** | Базовые контролы ZzzGUI | ⏳ Не начато | — | `Button`, `TextBlock`, `Image`, `Slider`, `ProgressBar`, `CheckBox`, `TextBox` (фокус/каретка) |
| **30** | 2D GPU GUI Batcher | ⏳ Не начато | — | Динамический вершинный буфер интерфейса (`GuiVertex2D`), прямоугольники отсечения Scissor Rects |
| **31** | Рендеринг текста в GUI | ⏳ Не начато | — | Выбор типа шрифта и способа отрисовки по факту (правило 21), API-обертки для движка и скриптов |
| **32** | Реактивный Data Binding Core | ⏳ Не начато | — | `ObservableProperty<T>`, команды `ICommand`, автоматический On-Demand перерендер только изменившихся элементов |
| **33** | 🚦 КП-3: кроссплатформенная проверка GUI-MVP | ⏳ Не начато | — | Проверка одинакового поведения интерфейса на Windows под DirectX 12 и Vulkan |
| **34** | 🚦 КП-4: портирование и проверка на Linux (Vulkan) | ⏳ Не начато | — | Куб + ZzzGUI-MVP на Linux (Vulkan) |

### Уровень 6: Мета-система ассетов, профили, Студия и сборка пакета
| № | Этап разработки | Статус | Файл этапа | Краткое описание |
|---|---|---|---|---|
| **35** | Параметры импорта ассетов в `.meta` | ⏳ Не начато | — | Типоспецифичные параметры импорта внутри `.meta`: текстуры (мипы, сжатие, фильтрация, sRGB), меши, материалы |
| **36** | Множественные платформенные JSON-профили конфигурации **(частично реализовано)** | ⏳ Не начато | — | Расширение существующей схемы `project_<platform>.json` под сборочные профили |
| **37** | Платформозависимые форматы собранных ресурсов | ⏳ Не начато | — | Базовый общий формат для MVP; тайринг качества — в Полировку |
| **38** | Бинарные форматы ресурсов и сборка `package.dat` **(частично реализовано)** | ⏳ Не начато | — | Добавление `MeshData`, `TextureData`, `MaterialData`, `ShaderData` в работающий `PackagePacker` |
| **39** | Студия проектов (ex-AssetsBuilder) **(частично реализовано)** | ⏳ Не начато | — | Доведение `tools/editor` и `tools/assets_builder` (инспектор `.zmat`, ссылки `FieldRef`) |
| **40** | Автоматическая доставка ресурсов в CMake | ⏳ Не начато | — | Post-build копирование собранного `package.dat` в папки запуска игровых таргетов |

### Уровень 7: Менеджеры ресурсов и сцен
| № | Этап разработки | Статус | Файл этапа | Краткое описание |
|---|---|---|---|---|
| **41** | `ResourceManager` | ⏳ Не начато | — | Асинхронные вызовы через лямбды (`LoadAsync(guid, callback)`), нулевые фризы кадра, fallback плейсхолдеры |
| **42** | Загрузка сцены в `SceneManager` **(частично реализовано)** | ⏳ Не начато | — | Расширение существующего `SceneManager` под асинхронную загрузку не-Primary окон |

### Уровень 8: Расширение платформенного покрытия
| № | Этап разработки | Статус | Файл этапа | Краткое описание |
|---|---|---|---|---|
| **43** | 🚦 КП-5: первый прогон на macOS и мобильных (Android/iOS, Metal) | ⏳ Не начато | — | Куб + ZzzGUI-MVP на macOS/iOS (Metal) и Android (Vulkan) |

### Уровень 9: Полировка / отложенная сложность
| № | Этап разработки | Статус | Файл этапа | Краткое описание |
|---|---|---|---|---|
| **44** | SIMD оптимизация матрицы `Mat4` | ⏳ Не начато | — | Аппаратное ускорение через SSE2/AVX и NEON без изменения публичного API |
| **45** | Менеджер CPU ядер и приоритетов пулов потоков | ⏳ Не начато | — | `CpuCoreManager`: топология CPU (P/E ядра), маски affinity, очереди задач без False Sharing (`alignas(64)`) и приоритеты тредпулов |
| **46** | Управление прозрачностью окна и альфа-каналом GAPI | ⏳ Не начато | — | Кроссплатформенная альфа окна; нативные Acrylic/Mica как надстройка |
| **47** | Платформозависимый сплэшскрин | ⏳ Не начато | — | Загрузочное окно старта (Win/Linux/macOS frameless splash, Android SplashScreen, iOS Storyboard) |
| **48** | Встраивание 3D и GPU-эффектов в UI (`Viewport3DControl`) | ⏳ Не начато | — | Интерактивные 3D объекты прямо в разметке UI, шейдерные эффекты, частицы и Glass/Acrylic блюр |
| **49** | Адаптивный ZzzGUI (Desktop vs Mobile) и полиморфизм | ⏳ Не начато | — | Стабильные Element ID, платформенные шаблоны разметки, полиморфизм UI-скриптов |
| **50** | Потоковое чтение тяжелых ресурсов (`ResourceStreaming`) | ⏳ Не начато | — | `IResourceStream`: потоковое чтение чанками (видео, длинное аудио) |
| **51** | Механизм смены сцен и визуальных эффектов перехода | ⏳ Не начато | — | Шейдерные эффекты перехода (Fade, Cross-Fade, Dissolve, Wipe, маски), захват кадра |

### Уровень 10: Финал
| № | Этап разработки | Статус | Файл этапа | Краткое описание |
|---|---|---|---|---|
| **52** | Итоговая документация и удаление плана | ⏳ Не начато | — | Формирование документации в `docs/` на базе файлов этапов, удаление `current_plan/` |
