# Генеральный план разработки: Вращающийся текстурированный 3D-куб (Textured Spinning Cube)

---

## 📜 Правила ведения и исполнения плана (Обязательно к соблюдению)

### Блок 1. Процесс разработки, этапы и верификация (Workflow & DoD)

1. **Никаких правок кода без явной команды:** Запрещено создавать или изменять файлы исходного кода до получения прямой команды от пользователя. Сначала каждый шаг и архитектурное решение подробно обсуждаются.
2. **Регламент изменений на лету:** Если в процессе реализации шага возникает необходимость изменить сигнатуру или расширить структуру — изменение сначала обсуждается и согласуется в чате, вносится в план текущего шага (`stage_XX_...md`), и только затем пишется код.
3. **Единый источник истины текущей работы (`stage_XX_...md`):**
   - Файл текущего шага всегда именуется по строгому шаблону `stage_<номер>_<имя>.md` (например, `stage_09_package_resource_formats.md`);
   - Содержит подробную техническую спецификацию, сигнатуры функций, форматы структур и чек-лист **только для текущего активного пункта**;
   - Для будущих (невыполненных) пунктов отдельные файлы планов **не создаются заранее**. Детальный план следующего этапа создается **строго по прямой команде пользователя** после завершения, валидации и утверждения предыдущего пункта;
   - После выполнения всех пунктов генерального плана составляется полная документация в `docs/` на основе накопленных файлов этапов, после чего папка `current_plan/` удаляется.
4. **Сверка с существующим кодом перед стартом пункта:** Перед началом работы над любым пунктом выполняется быстрый поиск по кодовой базе, чтобы определить, не реализовано ли уже частично нужное поведение. Решение «расширяем существующее» vs «переписываем» явно фиксируется в файле этапа.
5. **Критерий готовности шага (Definition of Done — DoD):**
   - Код написан строго по спецификации текущего этапа (`stage_XX_...md`);
   - Проект компилируется начисто без ошибок и предупреждений;
   - Необходимость и объём юнит-тестов определяются индивидуально для каждого шага при составлении `stage_XX_...md`. Если тесты предусмотрены — они написаны и 100% проходят;
   - Факт сборки и реального прогона подтверждён на платформах, актуальных для текущей стадии платформенного роллаута (Windows $\to$ Linux $\to$ macOS/мобильные);
   - Пользователь явно подтвердил: *«Шаг принят»*;
   - Только после подтверждения статус в `general_plan.md` меняется на `✅ Выполнено`;
   - Фиксируется атомарный Git-коммит (например, `git commit -m "feat(mesh): completed stage 09 - MeshData binary format, obj parser, and package packing"`), обеспечивающий мгновенный безопасный откат.

### Блок 2. Архитектура модулей, владение кодом и C++ стандарт

6. **Единый источник правды (Single Source of Truth):** Все константы, форматы данных, правила сериализации/десериализации, валидация путей и упаковка в `package.dat` реализуются **строго на C++ в нативных библиотеках движка** (`core`, `assets_builder_dll`). C# слой студии проектов служит исключительно графической оболочкой (UI) и вызывает нативный C/C++ API через P/Invoke без дублирования бизнес-логики.
7. **Модульность архитектуры библиотек:**
   - Низкоуровневые модули (`core_lib`, `math_lib`, `gapi_lib`, `gui_lib`) независимы и могут использоваться автономно для утилит и тестов;
   - **`engine_lib` остается главной высокоуровневой библиотекой для игр**, транзитивно объединяющей (через `PUBLIC` CMake-линковку) все подсистемы: игры подключают только `engine_lib` и получают сцены, ZzzGUI, рендер и скрипты;
   - UI-фреймворк движка временно именуется `ZzzGUI` (окончательное имя выбирается пользователем отдельно и заменяется сквозным проходом).
8. **Разделение POD-структур данных и сервисных классов (Value Objects vs Stateful Objects):**
   - **Математика, вертексы и дескрипторы данных (`src/math/`, `Vertex3D`, `Viewport`, DTO):** поля **открытые (`public`)**, структуры являются `Standard Layout POD`, чистые данные без скрытого состояния (`pt.x`, `sz.width`, `v.z`, `rect.position`);
   - **Подсистемы, менеджеры, компоненты и контролы (`RenderManager`, `Camera`, `Transform`, `UIElement`):** **строгая инкапсуляция (поля `private` / `protected`)**, доступ только через методы с валидацией инвариантов и контролем жизненного цикла.

### Блок 3. Кроссплатформенность и изоляция платформ (Zero Platform Leaks)

9. **Строгая изоляция платформенного кода:** В кроссплатформенных модулях (`src/math/`, `src/gui/`, `src/engine/`, `src/core/`) категорически запрещено прямое подключение платформозависимых заголовков (`<windows.h>`, `<d3d12.h>`, `<vulkan/vulkan.h>`, `<Metal/Metal.h>`). Вся нативная специфика строго изолирована в поддиректориях `platforms/` и `gapi/`.
10. **Платформенная специализация через Compile-Time Type Aliases (Zero `#ifdef` Spaghetti):** Для платформозависимых подсистем (`Input`, `MainLoop`, `FileSystem`, структуры конфигураций) запрещено размазывать директивы `#ifdef` внутри тел методов:
    - Базовый класс (`XxxBase`) содержит общую кроссплатформенную логику (чистый C++23, 0 дефайнов внутри методов);
    - Платформенные классы (`platforms/XxxPlatform`) инкапсулируют нативные API ОС в отдельных изолированных файлах. Если между десктопными ОС нет различий в используемых API (например, стандартный дисковый I/O C++23 в `FileSystem`), Windows, Linux и macOS **сводятся в один класс `XxxDesktop`** без дублирования кода. Разделение на `XxxMSWin`, `XxxLinux`, `XxxMacOS` выполняется **только при реальной специфике нативных API ОС** (события окон, ввод Win32 vs Wayland vs Cocoa);
    - Для мобильных ОС (`platforms/XxxAndroid`, `platforms/XxxiOS`) создаются изолированные специализации под нативные SDK;
    - Центральный заголовочный файл (`Xxx.h`) экспортирует тип через `using Xxx = XxxPlatform;` (или `XxxDesktop`). Это гарантирует Zero-Cost Abstraction (без виртуальных вызовов и лишних аллокаций) и полную изоляцию нативных SDK.
11. **Стандарт синтаксиса и полная изоляция платформенных файлов (Full-File Platform Guards):**
    - Для всех проверок платформ и GAPI в директивах препроцессора строго используется синтаксис `#if defined(...)` (например, `#if defined(Z_WINDOWS)`, `#if defined(Z_LINUX)`, `#if defined(Z_D3D12)`, `#if defined(Z_VULKAN)`, `#if defined(Z_METAL)`), исключающий предупреждения компилятора `-Wundef`;
    - Любой платформозависимый файл (`.h`, `.cpp`, `.mm`) оборачивается в `#if defined(...)` **целиком**: от директивы `#include "core/utils/Defines.h"` до конца файла (`#endif // defined(...)`). При компиляции под другую ОС файл превращается в пустую единицу трансляции (0 байт), исключая конфликты линковки.
    - *Исключение:* платформенные DTO-структуры сериализации данных пакета (`ViewData*.h`, `ProjectPlatformData*.h`), так как десктопная утилита `PackagePacker` должна упаковывать манифесты для всех платформ.
12. **Единый стандарт системы координат и порядка обхода вершин (Left-Handed & CW Winding Order):** В движке принят единый стандарт — **Левосторонняя система координат (Y-up, Z-forward, диапазон глубины NDC Z: $[0, 1]$)** и **порядок обхода вершин по часовой стрелке (Clockwise, CW)** для лицевых граней при взгляде снаружи. Все 3D-ассеты в пакетах хранятся строго в этом порядке, а бэкенды GAPI настраивают растеризатор аппаратно:
    - DirectX 12: `FrontCounterClockwise = FALSE` (CW);
    - Vulkan: `VK_FRONT_FACE_CLOCKWISE` (CW), компенсация инвертированной оси Y через отрицательную высоту вьюпорта (`viewport.y = height; viewport.height = -height;`);
    - Metal: `[encoder setFrontFacingWinding:MTLWindingClockwise]` (CW).
    Благодаря этому матрицы трансформации и геометрия в памяти одинаковы для всех целевых платформ без конвертаций на лету.

### Блок 4. Производительность, Concurrency и Data-Oriented Design (DOD)

13. **Data-Oriented Design (DOD), Cache Locality и Zero-Allocation в горячем цикле кадра:**
    - Конвейер рендеринга, скриптов и компонентов проектируется по принципам DOD: данные хранятся в непрерывных массивах (`std::vector`) для максимального L1/L2 Cache Hit;
    - Очереди отрисовки используют линейные арена-аллокаторы кадра (Frame Arena);
    - Межпоточные структуры (очереди кадров $N / N-1$, `DoubleBufferedVector`, кольцевые буферы и счётчики пула задач) выравниваются через `alignas(std::hardware_destructive_interference_size)` (64 байта) для исключения False Sharing между ядрами CPU.
14. **Многопоточная изоляция кадра через неизменяемый снимок (Immutable Render Snapshot & Zero-Lock):**
    Поток рендеринга (Render Thread) категорически не обращается к живым объектам игровой сцены (`GameObject`, `Transform`, `Scene`, `ObjectWorld`). Потокобезопасность кадрового конвейера достигается физической изоляцией данных во времени и памяти:
    - **Фаза экстракции снимка:** В конце кадрового цикла `Update(dt)` поток логики сам распаковывает иерархическое дерево сцены в плоский линейный буфер команд кадра (`RenderQueue` / `RenderCommand`);
    - **Неизменяемость снимка:** После передачи снимка в рендер-поток через атомарный сдвиг кадровых буферов (`SwapQueue::Swap`) данные кадра становятся строго read-only для потока рендеринга;
    - **Параллелизм без блокировок (Zero-Lock):** Поток логики крутит следующий кадр $N+1$ в независимом буфере подготовки, пока поток рендеринга параллельно читает монолитный снимок кадра $N$. Это исключает гонки данных (Data Races), кэш-промахи и задержки ожидания мьютексов.
15. **Стратегия обработки ошибок и контракты производительности:**
    - **Контрактные проверки разработки (`ensure`):** Для валидации инвариантов классов, аргументов функций и готовности подсистем используется встроенный `ensure(condition, message)` из `src/core/utils/Ensure.h` (работает в Debug/Dev через `source_location`, в Release компилируется в zero-overhead no-op);
    - **Горячие циклы кадра (`Update`, `PrepareFrame`, `Submit`):** Никаких тяжелых C++ исключений (`throw`), только быстрые коды возврата, `ensure` и логгер;
    - **Восстанавливаемые ошибки ввода-вывода (VFS, загрузка ресурсов):** Использование `std::expected<T, eErrorCode>` и кодов ошибок;
    - **GUID как доверенный контрактом идентификатор:** GUID, приходящий из уже провалидированных данных пакета (`SceneData`, `GameObjectData` и т.п.) или напрямую от кода движка, по контракту гарантированно валиден и разрешается в `package.dat` — на сайтах потребления (например, `SceneManager::LoadSceneAsync(Guid, ...)`) НЕ добавляются защитные `ensure` на пустоту или существование самого GUID. Исключение — разрешение по человеко-вводимому идентификатору (поиск по строковому имени, ввод из UI/скриптов): там явная проверка результата поиска обязательна.

### Блок 5. Минимализм API и соблюдение YAGNI (You Aren't Gonna Need It)

16. **Минимизация оверинжиниринга и Just-In-Time разработка:**
    Категорически запрещено реализовывать методы, структуры, вспомогательные функции, обёртки и геттеры «про запас», «на будущее» или «для полноты API», если у них нет конкретного реального потребителя прямо сейчас в кодовой базе:
    - **Публичный контракт классов:** содержит строго минимальный набор методов, необходимый для выполнения текущих задач движка;
    - **Герметичность инкапсуляции:** запрещено добавлять «удобные» геттеры к внутренним подсистемам, если они никем не вызываются или пробивают инкапсуляцию;
    - **Тесты как зеркало контракта:** тесты не служат оправданием существования мертвого кода (код пишется для работы движка, а не ради тестов);
    - **Контроль за разрастанием типов (Type Sprawl Control):** появление новых типов, обёрток и коротких type-алиасов (`XxxF`/`XxxD`/`XxxU8`) не выполняется по умолчанию «для единообразия» — каждый новый тип явно согласуется с пользователем;
    - **Технология не фиксируется заранее:** для возможностей, реализация которых не критична прямо сейчас (шрифты, блюр/частицы, алгоритмы сжатия), генеральный план фиксирует ЧТО нужно, а конкретная технология выбирается непосредственно перед реализацией в файле этапа.

### Блок 6. Форматы данных, версионирование и QA-инфраструктура

17. **Строгая топологическая последовательность и версионирование форматов:**
    - Каждый пункт расположен так, что все необходимые ему классы, структуры и подсистемы уже созданы и протестированы на предыдущих шагах;
    - При проектировании текущего пункта учитываются точки расширения для последующих этапов (Forward Compatibility) без написания лишнего кода;
    - При изменении форматов сериализации (`package.dat`, `.meta`, `.zmat`, `user.dat`) обязателен явный план миграции/совместимости, чтобы старые файлы не ломались молча.
18. **Постоянный тестовый проект и централизованное управление тестами:**
    - `src/projects/assets_projects/zzz_assets_test_000` — постоянное хранилище тестовых ассетов и сцен для QA; тестовые ассеты не создаются одноразово и не удаляются после проверки шага;
    - Файлы тестов раскладываются строго по тематическим подпапкам модуля (`src/qa/tests/math/`, `src/qa/tests/core/`, `src/qa/tests/gapi/`);
    - Включение и отключение наборов тестов управляется централизованно через единый файл **`src/qa/tests/TestsConfig.h`** директивами `#define Z_TEST_...` без ручного редактирования или удаления файлов тестов.

---

## 📚 Референсный проект с примерами реализации
> **Путь к референсному проекту:** [`c:\Workspaces\DZzz`](file:///c:/Workspaces/DZzz)  
> Содержит примеры шаблонного буфера вершин (`CPUVertexBuffer.cppm`), маппера форматов (`VertexFormatMapper.cppm`) и другие реализации подсистем Zzz.

---

## 🎯 Финальная цель проекта
Обеспечить полный цикл работы графической и ресурсной подсистем ZzzEngine: от добавления исходных ассетов в проект до отображения и **вращения текстурированного 3D-куба пользовательским скриптом** в окне приложения через кроссплатформенный GAPI (DirectX 12 / Vulkan / Metal).  
Рендеринг работает по строгой многопоточной схеме кадрового конвейера: **в потоке $N$ подготавливаются данные сцены и записываются команды, в потоке $N-1$ команды исполняются на GPU и выводятся на экран**.

> **💡 Главная миссия Special GUI Layer (ZzzGUI в `gui_lib`):** Создание высокопроизводительного автономного MVVM UI-фреймворка на нативном GAPI с возможностью прямого бесшовного встраивания интерактивных **3D объектов и GPU-эффектов (частицы, свечение, Glass/Acrylic блюр) прямо в элементы интерфейса** с On-Demand рендерингом десктопного уровня.


## 🧭 Приоритеты MVP (уточнено пользователем, 2026-08-31)

1. **Главный критерий приёмки любого пункта — идентичное поведение сборки на всех целевых платформах.** Не «скомпилировалось на Windows», а «прогнано и одинаково выглядит/работает» (см. новые контрольные точки кроссплатформенной проверки в таблице ниже).
2. **ZzzGUI (кроссплатформенный WPF-подобный UI-фреймворк) важнее полноты 3D-графики.** При конфликте приоритетов между «доделать графический эффект» и «довести GUI до одинаковой работы на всех платформах» — приоритет у GUI.
3. **Графика на первом проходе — минимально достаточная, а не максимально красивая.** Технологии, не критичные прямо сейчас (конкретная реализация шрифтов, блюр/частицы, переходы сцен), не фиксируются заранее — решаются в момент подхода к пункту (правило 16). Ничего не исключается из плана заранее без явного решения пользователя.

## 🗺️ Платформенный роллаут (уточнено пользователем, 2026-08-31)

Платформы вводятся по очереди, не все сразу — со своими контрольными точками (КП) в таблице ниже:

1. **Windows** (DirectX12 + Vulkan) — основная платформа разработки; все КП-1..КП-3 проверяются здесь.
2. **Linux** (Vulkan) — следующая цель сразу после подтверждения куба и ZzzGUI-MVP на Windows (КП-4).
3. **macOS и мобильные (Android/iOS, Metal)** — позже (КП-5). *Предположение: macOS сгруппирован с мобильными платформами, т.к. явно не назван пользователем отдельно и разделяет бэкенд Metal с iOS — уточнить при необходимости.*

---

### 📌 Текущее состояние разработки

> [!IMPORTANT]
> **Текущий активный пункт:** `Пункт 10. Подсистема ресурсов (ResourceManager, ResourceGarbageCollector, DoubleBufferedVector)`  
> **Статус:** ⏳ Не начато  
> **Файл детального плана текущего шага:** [`stage_10_resource_manager.md`](stage_10_resource_manager.md)  
> **Список открытых сквозных задач / технического долга:** [`docs/ARCHITECTURE.md`](../docs/ARCHITECTURE.md) §4  
> **Текущая подзадача:** Проектирование архитектуры `ResourceManager`, `IResource`, фонового сборщика `ResourceGarbageCollector` и потокобезопасного кэширования.

---

## 📋 Сквозная таблица этапов реализации (Строгая последовательность зависимостей)

### Уровень 1: Математический, файловый и платформенный фундамент
| № | Этап разработки | Статус | Файл этапа | Краткое описание |
|---|---|---|---|---|
| **1** | Векторная математика (`Vec2`, `Vec3`, `Vec4`) и унификация `Point2D` | ✅ Выполнено | [`stage_01_vectors.md`](stage_01_vectors.md) | Шаблоны (`zF32`/`zI32`/`zU32`), C++23 концепты, операции, открытые x/y в `Point2D`, сериализация, `std::formatter` |
| **2** | Матрицы `Mat4` и `Mat3` (базовые операции, аффинные трансформации, базис и проекции) | ✅ Выполнено | [`stage_02_mat4.md`](stage_02_mat4.md) | Умножение ($v \cdot M$), $TRS$ ($Z \to X \to Y$), `LookAtLH`, `PerspectiveFovLH` $[0, 1]$, `Mat3` базис и матрица нормалей, сериализация |
| **3** | Кватернионы `Quat` | ✅ Выполнено | [`stage_03_quat.md`](stage_03_quat.md) | 3D вращения без Gimbal Lock, `Slerp`, `FromEuler`/`ToEuler`, `ToMat4`, сериализация |
| **4** | Базовые перечисления (Enums) и структуры ядра | ✅ Выполнено | [`stage_04_enums_and_core_structs.md`](stage_04_enums_and_core_structs.md) | `eResourceType`, `ePixelFormat`, `eIndexFormat`, `ConverterGAPITypes`, `eVertexSemantic`, `Vertex3D`, `AttributeRange`, выравнивание CBV (256б), `EnumToString` |
| **5** | Кроссплатформенная файловая система (`FileSystem`, `eFileLocation`) | ✅ Выполнено | [`stage_05_filesystem.md`](stage_05_filesystem.md) | Compile-Time Type Alias (`using FileSystem = ...`), `FileSystemBase`, `FileSystemDesktop`, `eFileLocation`, ranged-read, изоляция `Path` |
| **6** | Рефакторинг подсистемы аппаратуры (`HardwareManager`, `HardwareState`) | ✅ Выполнено | [`stage_06_hardware_manager.md`](stage_06_hardware_manager.md) | Вынос сбора телеметрии (CPU, RAM, GPU, Storage, Network) из монолитного `Platform::GatherHardwareState` в чистый сервис по Правилу 21 (SRP, изоляция нативных SDK Win32 DXGI/SMBIOS) |

### Уровень 2: GAPI-ресурсы, содержимое куба и сквозной рендер
| № | Этап разработки | Статус | Файл этапа | Краткое описание |
|---|---|---|---|---|
| **7** | Сущность сцены (`GameObject`), иерархический `Transform`, `SlotMap`, слои (`ILayer`, `Layer3D`, `LayerUI`, `LayerMVVM`) и `ISceneStorage` | ✅ Выполнено | [`stage_07_gameobject_and_transform.md`](stage_07_gameobject_and_transform.md) | `Transform` (поле-значение, dirty flag), `GameObject`, `SlotMap<T>` (Swap & Pop, stable handles), слои сцены (`ILayer`, `Layer3D`, `LayerUI`, `LayerMVVM`), `ISceneStorage` (`DefaultSceneStorage`), `GameObjectData` |
| **8** | Конвейер смены сцен, асинхронная загрузка и переходы (`SceneTransitionParams`, `SceneManager::LoadSceneAsync`, `View::SetScene`) | ✅ Выполнено | [`stage_08_scene_transition_pipeline.md`](stage_08_scene_transition_pipeline.md) | Вынос загрузки сцены из конструктора `View`, `SceneTransitionParams` (структура с флагами перехода, `blockUserInput`), `View::SetScene`, асинхронная загрузка (фоновые I/O, десериализация и создание `Scene` в `ThreadPool` + потокобезопасная публикация в `m_Scenes`, запуск скриптов `InvokeStart` и вызов `onComplete` на логическом потоке через `m_MainThreadQueue` / `CallbackQueue`) |
| **9** | Сквозной конвейер сетки: бинарный формат `MeshData`, парсинг `.obj`, упаковка и загрузка из `package.dat` | ✅ Выполнено | [`stage_09_package_resource_formats.md`](stage_09_package_resource_formats.md) | `MeshData` в `core`, парсер `.obj` в `assets_builder_dll`, упаковка в `package.dat` и `data.dat`, исходник `cube_00.obj` с `.meta`, UTF-32 имена, белый список расширений |
| **9.1** | Менеджер проектов Сборщика Ассетов: каркас проекта, валидация структуры, пресеты сборки, платформенные конфиги и дельты | ⏳ В процессе | [`stage_09_1_assets_builder_presets_and_scaffolding.md`](stage_09_1_assets_builder_presets_and_scaffolding.md) | Создание каркаса в пустой папке (Scaffolding), строгая валидация, наборы сборки (`presets.json`), платформенные конфиги с версиями сторов и дельтами `add_*`/`remove_*`, очистка `project.json` от дублей |
| **10** | Подсистема ресурсов (`ResourceManager`, `ResourceGarbageCollector`, `DoubleBufferedVector`) | ⏳ Не начато | [`stage_10_resource_manager.md`](stage_10_resource_manager.md) | Базовый интерфейс `IResource`, `eResourceState`, кэш по типам (Mesh, Texture, Shader, Material), выделенный I/O-поток, фоновый сборщик `ResourceGarbageCollector` с RAII `ScopedGCSuspension`, интеграция в `Engine` |
| **11** | Шаблонная система буфера вершин и маппер GAPI | ⏳ Не начато | — | `CPUVertexBuffer<Attrs...>`, `eVertexFormat`, `ConverterGAPITypes` и `VertexFormatMapper` (референс: `c:\Workspaces\DZzz\...`) |
| **12** | Кроссплатформенные абстракции ресурсов GAPI | ⏳ Не начато | — | `IVertexBuffer`, `IIndexBuffer`, `IConstantBuffer` (двойная буферизация кадра), `ITexture2D` |
| **13** | Управление зоной рендеринга и DPI-масштабирование | ⏳ Не начато | — | Viewport, Scissor, Safe Area, логические единицы (DIP / Canvas Units), компенсация Y в Vulkan |
| **14** | Компонент `Camera` | ⏳ Не начато | — | Углы обзора, View/Projection матрицы, автоматический пересчет Aspect при ресайзе |
| **15** | Структура и ресурс `Mesh` и генератор 3D-куба | ⏳ Не начато | — | Ресурс `Mesh : public IResource`, `MeshLoader`, единичный куб: 24 вершины (нормали, UV), 36 индексов |
| **16** | Класс и ресурс `Material` и формат `.zmat` | ⏳ Не начато | — | Ресурс `Material : public IResource`, динамический Property Bag (`SetColor`, `SetFloat`, `SetTexture`), файл материала |
| **17** | Базовый текстурный шейдер `BasicTextured.hlsl` | ⏳ Не начато | — | HLSL шейдер с текстурированием и простым рассеянным освещением |
| **18** | Формирование команд отрисовки меша и кадровой очереди (`RenderCommand`, `RenderQueue`) | ⏳ Не начато | — | Экстракция данных из `GameObject` (Transform, Mesh, Material) в плоские команды отрисовки `RenderCommand` и сборка неизменяемого снимка кадра `RenderQueue` для потока рендеринга (Правило 28) |
| **19** | Отрисовка куба на DX12/Vulkan/Metal **(частично реализовано)** | ⏳ Не начато | — | Доведение `SurfView_DX/VK/Metal` и `RenderManager` до реальной отрисовки текстурированного куба |
| **20** | Компиляция шейдеров и кэширование PSO | ⏳ Не начато | — | Сборка через `dxc.exe` в DXIL и SPIR-V, `VkPipelineCache` и `ID3D12PipelineLibrary` |
| **21** | 🚦 КП-1: кроссплатформенная проверка статичного куба | ⏳ Не начато | — | Сборка и проверка отрисовки куба на Windows (DirectX12 + Vulkan) |

### Уровень 3: Скриптование и первая живая проверка вращения
| № | Этап разработки | Статус | Файл этапа | Краткое описание |
|---|---|---|---|---|
| **22** | Механизм скриптования `GameObject` по GUID **(частично реализовано)** | ⏳ Не начато | — | Доведение существующей `ScriptFactory`/`ScriptRegistry`/`ScriptStorage` под Transform/Mesh/Material |
| **23** | Фасады движка для скриптов (Engine Facades) | ⏳ Не начато | — | Безопасные обертки API (`EngineFacade`, `SceneFacade`, `ViewFacade`) с разграничением прав |
| **24** | Изоляция и контроль видимости скриптов | ⏳ Не начато | — | Виртуальные методы ограничения доступа к скриптам верхнего уровня для нижележащих скриптов |
| **25** | Вращение куба пользовательским скриптом | ⏳ Не начато | — | Скрипт `CubeRotatorScript` на объекте куба, вращение во времени (`DeltaTime`) |
| **26** | 🚦 КП-2: кроссплатформенная проверка вращающегося куба | ⏳ Не начато | — | Тестирование параллельной схемы кадрового конвейера ($N$ готовит / $N-1$ рендерит) на Windows (DX12 + VK) |
| **27** | Сериализуемые поля и ссылки скриптов (`FieldRef`) | ⏳ Не начато | — | Unity-like поля в скриптах: ссылки на `GameObject`, другие скрипты и ассеты с 2-фазной линковкой |

### Уровень 4: Продвинутые пространственные контейнеры и кадровые режимы слоев
| № | Этап разработки | Статус | Файл этапа | Краткое описание |
|---|---|---|---|---|
| **28** | Продвинутые структуры пространственного хранения объектов (`ISceneStorage`) | ⏳ Не начато | — | Реализация AABB-деревьев (`BVHSceneStorage`, `QuadTreeSceneStorage`, `OctreeSceneStorage`), Frustum Culling, автопересчёт границ |
| **29** | Независимая кадровика слоёв и On-Demand рендеринг | ⏳ Не начато | — | Развитие `Layer3D`, `LayerUI`, `LayerMVVM`: независимый FPS для слоёв, On-Demand отрисовка для UI, частицы и 3D в интерфейсе |

### Уровень 5: Ввод и UI-фреймворк ZzzGUI
| № | Этап разработки | Статус | Файл этапа | Краткое описание |
|---|---|---|---|---|
| **30** | Автоматическая маршрутизация ввода в слои сцены | ⏳ Не начато | — | Доставка Тач / Мышь / Клавиатура / Геймпад: Hit-Testing в GUI с поглощением кликов и проброс в 3D World |
| **31** | Движок разметки ZzzGUI (`Measure`/`Arrange`) | ⏳ Не начато | — | CMake-таргет `gui_lib`: двухпроходный layout, контейнеры `StackPanel`, `Grid`, `Canvas`, `ScrollViewer`, `Border` |
| **32** | Базовые контролы ZzzGUI | ⏳ Не начато | — | `Button`, `TextBlock`, `Image`, `Slider`, `ProgressBar`, `CheckBox`, `TextBox` (фокус/каретка) |
| **33** | 2D GPU GUI Batcher | ⏳ Не начато | — | Динамический вершинный буфер интерфейса (`GuiVertex2D`), прямоугольники отсечения Scissor Rects |
| **34** | Рендеринг текста в GUI | ⏳ Не начато | — | Выбор типа шрифта и способа отрисовки по факту (правило 16), API-обертки для движка и скриптов |
| **35** | Реактивный Data Binding Core | ⏳ Не начато | — | `ObservableProperty<T>`, команды `ICommand`, автоматический On-Demand перерендер только изменившихся элементов |
| **36** | 🚦 КП-3: кроссплатформенная проверка GUI-MVP | ⏳ Не начато | — | Проверка одинакового поведения интерфейса на Windows под DirectX 12 и Vulkan |
| **37** | 🚦 КП-4: портирование и проверка на Linux (Vulkan) | ⏳ Не начато | — | Куб + ZzzGUI-MVP на Linux (Vulkan) |

### Уровень 6: Мета-система ассетов, профили, Студия и сборка пакета
| № | Этап разработки | Статус | Файл этапа | Краткое описание |
|---|---|---|---|---|
| **38** | Параметры импорта ассетов в `.meta` | ⏳ Не начато | — | Типоспецифичные параметры импорта внутри `.meta`: текстуры (мипы, сжатие, фильтрация, sRGB), меши, материалы |
| **39** | Множественные платформенные JSON-профили конфигурации **(частично реализовано)** | ⏳ Не начато | — | Расширение существующей схемы `project_<platform>.json` под сборочные профили |
| **40** | Платформозависимые форматы собранных ресурсов | ⏳ Не начато | — | Базовый общий формат для MVP; тайринг качества — в Полировку |
| **41** | Продвинутая компиляция ресурсов и оптимизация `package.dat` **(частично реализовано)** | ⏳ Не начато | — | Сжатие текстур (BC1-BC7 / ASTC), компиляция шейдеров в `PackagePacker` |
| **42** | Студия проектов (ex-AssetsBuilder) **(частично реализовано)** | ⏳ Не начато | — | Доведение `tools/editor` и `tools/assets_builder` (инспектор `.zmat`, ссылки `FieldRef`) |
| **43** | Автоматическая доставка ресурсов в CMake | ⏳ Не начато | — | Post-build копирование собранного `package.dat` в папки запуска игровых таргетов |

### Уровень 7: Менеджеры сцен и загрузка
| № | Этап разработки | Статус | Файл этапа | Краткое описание |
|---|---|---|---|---|
| **44** | Загрузка сцены в `SceneManager` **(частично реализовано)** | ⏳ Не начато | — | Расширение существующего `SceneManager` под асинхронную загрузку не-Primary окон через `ResourceManager` |

### Уровень 8: Расширение платформенного покрытия
| № | Этап разработки | Статус | Файл этапа | Краткое описание |
|---|---|---|---|---|
| **45** | 🚦 КП-5: первый прогон на macOS и мобильных (Android/iOS, Metal) | ⏳ Не начато | — | Куб + ZzzGUI-MVP на macOS/iOS (Metal) и Android (Vulkan) |

### Уровень 9: Полировка / отложенная сложность
| № | Этап разработки | Статус | Файл этапа | Краткое описание |
|---|---|---|---|---|
| **46** | SIMD оптимизация матрицы `Mat4` | ⏳ Не начато | — | Аппаратное ускорение через SSE2/AVX и NEON без изменения публичного API |
| **47** | Менеджер CPU ядер и приоритетов пулов потоков | ⏳ Не начато | — | `CpuCoreManager`: топология CPU (P/E ядра), маски affinity, очереди задач без False Sharing (`alignas(64)`) и приоритеты тредпулов |
| **48** | Управление прозрачностью окна и альфа-каналом GAPI | ⏳ Не начато | — | Кроссплатформенная альфа окна; нативные Acrylic/Mica как надстройка |
| **49** | Платформозависимый сплэшскрин | ⏳ Не начато | — | Загрузочное окно старта (Win/Linux/macOS frameless splash, Android SplashScreen, iOS Storyboard) |
| **50** | Встраивание 3D и GPU-эффектов в UI (`Viewport3DControl`) | ⏳ Не начато | — | Интерактивные 3D объекты прямо в разметке UI, шейдерные эффекты, частицы и Glass/Acrylic блюр |
| **51** | Адаптивный ZzzGUI (Desktop vs Mobile) и полиморфизм | ⏳ Не начато | — | Стабильные Element ID, платформенные шаблоны разметки, полиморфизм UI-скриптов |
| **52** | Потоковое чтение тяжелых ресурсов (`ResourceStreaming`) | ⏳ Не начато | — | `IResourceStream`: потоковое чтение чанками (видео, длинное аудио) |
| **53** | Механизм смены сцен и визуальных эффектов перехода | ⏳ Не начато | — | Шейдерные эффекты перехода (Fade, Cross-Fade, Dissolve, Wipe, маски), захват кадра |

### Уровень 10: Финал
| № | Этап разработки | Статус | Файл этапа | Краткое описание |
|---|---|---|---|---|
| **54** | Итоговая документация и удаление плана | ⏳ Не начато | — | Формирование документации в `docs/` на базе файлов этапов, удаление `current_plan/` |
